# 4.1 DOM: поиск, создание и изменение узлов

- Домен: **DOM и Browser**
- Атомарных навыков: **130**
- Тип: **Hard**
- Технология: **Browser**
- Описание технологии: Платформа браузера, DevTools и Web API.

## Навыки

| # | Навык | Описание | Индикатор развития | Критерии успеха | Проверка | Инструмент развития |
|---|---|---|---|---|---|---|
| 1 | document.querySelector — базовое понимание | Понимает назначение и базовое поведение API/механизма «document.querySelector» в браузере. | Путает назначение «document.querySelector» или не знает, когда его применять. | Осознанно выбирает «document.querySelector» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «document.querySelector» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 2 | document.querySelector — практическое применение | Умеет применять «document.querySelector» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «document.querySelector» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «document.querySelector» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «document.querySelector». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 3 | document.querySelector — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «document.querySelector». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «document.querySelector». | Предусматривает ограничения и проектирует безопасное поведение вокруг «document.querySelector». | Найти баг, связанный с темой «document.querySelector», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 4 | document.querySelector — отладка и измерение | Умеет отлаживать «document.querySelector» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «document.querySelector» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «document.querySelector». | Показать, где в DevTools искать проблему по теме «document.querySelector». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 5 | document.querySelector — проектная интеграция | Понимает, как встроить «document.querySelector» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «document.querySelector» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «document.querySelector» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «document.querySelector» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 6 | document.querySelectorAll — базовое понимание | Понимает назначение и базовое поведение API/механизма «document.querySelectorAll» в браузере. | Путает назначение «document.querySelectorAll» или не знает, когда его применять. | Осознанно выбирает «document.querySelectorAll» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «document.querySelectorAll» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 7 | document.querySelectorAll — практическое применение | Умеет применять «document.querySelectorAll» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «document.querySelectorAll» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «document.querySelectorAll» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «document.querySelectorAll». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 8 | document.querySelectorAll — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «document.querySelectorAll». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «document.querySelectorAll». | Предусматривает ограничения и проектирует безопасное поведение вокруг «document.querySelectorAll». | Найти баг, связанный с темой «document.querySelectorAll», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 9 | document.querySelectorAll — отладка и измерение | Умеет отлаживать «document.querySelectorAll» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «document.querySelectorAll» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «document.querySelectorAll». | Показать, где в DevTools искать проблему по теме «document.querySelectorAll». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 10 | document.querySelectorAll — проектная интеграция | Понимает, как встроить «document.querySelectorAll» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «document.querySelectorAll» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «document.querySelectorAll» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «document.querySelectorAll» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 11 | getElementById — базовое понимание | Понимает назначение и базовое поведение API/механизма «getElementById» в браузере. | Путает назначение «getElementById» или не знает, когда его применять. | Осознанно выбирает «getElementById» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «getElementById» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 12 | getElementById — практическое применение | Умеет применять «getElementById» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «getElementById» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «getElementById» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «getElementById». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 13 | getElementById — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «getElementById». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «getElementById». | Предусматривает ограничения и проектирует безопасное поведение вокруг «getElementById». | Найти баг, связанный с темой «getElementById», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 14 | getElementById — отладка и измерение | Умеет отлаживать «getElementById» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «getElementById» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «getElementById». | Показать, где в DevTools искать проблему по теме «getElementById». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 15 | getElementById — проектная интеграция | Понимает, как встроить «getElementById» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «getElementById» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «getElementById» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «getElementById» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 16 | getElementsByClassName — базовое понимание | Понимает назначение и базовое поведение API/механизма «getElementsByClassName» в браузере. | Путает назначение «getElementsByClassName» или не знает, когда его применять. | Осознанно выбирает «getElementsByClassName» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «getElementsByClassName» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 17 | getElementsByClassName — практическое применение | Умеет применять «getElementsByClassName» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «getElementsByClassName» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «getElementsByClassName» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «getElementsByClassName». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 18 | getElementsByClassName — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «getElementsByClassName». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «getElementsByClassName». | Предусматривает ограничения и проектирует безопасное поведение вокруг «getElementsByClassName». | Найти баг, связанный с темой «getElementsByClassName», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 19 | getElementsByClassName — отладка и измерение | Умеет отлаживать «getElementsByClassName» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «getElementsByClassName» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «getElementsByClassName». | Показать, где в DevTools искать проблему по теме «getElementsByClassName». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 20 | getElementsByClassName — проектная интеграция | Понимает, как встроить «getElementsByClassName» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «getElementsByClassName» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «getElementsByClassName» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «getElementsByClassName» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 21 | matches() — базовое понимание | Понимает назначение и базовое поведение API/механизма «matches()» в браузере. | Путает назначение «matches()» или не знает, когда его применять. | Осознанно выбирает «matches()» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «matches()» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 22 | matches() — практическое применение | Умеет применять «matches()» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «matches()» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «matches()» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «matches()». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 23 | matches() — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «matches()». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «matches()». | Предусматривает ограничения и проектирует безопасное поведение вокруг «matches()». | Найти баг, связанный с темой «matches()», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 24 | matches() — отладка и измерение | Умеет отлаживать «matches()» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «matches()» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «matches()». | Показать, где в DevTools искать проблему по теме «matches()». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 25 | matches() — проектная интеграция | Понимает, как встроить «matches()» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «matches()» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «matches()» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «matches()» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 26 | closest() — базовое понимание | Понимает назначение и базовое поведение API/механизма «closest()» в браузере. | Путает назначение «closest()» или не знает, когда его применять. | Осознанно выбирает «closest()» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «closest()» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 27 | closest() — практическое применение | Умеет применять «closest()» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «closest()» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «closest()» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «closest()». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 28 | closest() — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «closest()». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «closest()». | Предусматривает ограничения и проектирует безопасное поведение вокруг «closest()». | Найти баг, связанный с темой «closest()», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 29 | closest() — отладка и измерение | Умеет отлаживать «closest()» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «closest()» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «closest()». | Показать, где в DevTools искать проблему по теме «closest()». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 30 | closest() — проектная интеграция | Понимает, как встроить «closest()» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «closest()» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «closest()» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «closest()» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 31 | createElement — базовое понимание | Понимает назначение и базовое поведение API/механизма «createElement» в браузере. | Путает назначение «createElement» или не знает, когда его применять. | Осознанно выбирает «createElement» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «createElement» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 32 | createElement — практическое применение | Умеет применять «createElement» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «createElement» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «createElement» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «createElement». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 33 | createElement — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «createElement». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «createElement». | Предусматривает ограничения и проектирует безопасное поведение вокруг «createElement». | Найти баг, связанный с темой «createElement», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 34 | createElement — отладка и измерение | Умеет отлаживать «createElement» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «createElement» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «createElement». | Показать, где в DevTools искать проблему по теме «createElement». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 35 | createElement — проектная интеграция | Понимает, как встроить «createElement» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «createElement» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «createElement» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «createElement» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 36 | append — базовое понимание | Понимает назначение и базовое поведение API/механизма «append» в браузере. | Путает назначение «append» или не знает, когда его применять. | Осознанно выбирает «append» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «append» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 37 | append — практическое применение | Умеет применять «append» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «append» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «append» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «append». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 38 | append — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «append». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «append». | Предусматривает ограничения и проектирует безопасное поведение вокруг «append». | Найти баг, связанный с темой «append», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 39 | append — отладка и измерение | Умеет отлаживать «append» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «append» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «append». | Показать, где в DevTools искать проблему по теме «append». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 40 | append — проектная интеграция | Понимает, как встроить «append» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «append» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «append» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «append» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 41 | prepend — базовое понимание | Понимает назначение и базовое поведение API/механизма «prepend» в браузере. | Путает назначение «prepend» или не знает, когда его применять. | Осознанно выбирает «prepend» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «prepend» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 42 | prepend — практическое применение | Умеет применять «prepend» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «prepend» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «prepend» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «prepend». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 43 | prepend — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «prepend». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «prepend». | Предусматривает ограничения и проектирует безопасное поведение вокруг «prepend». | Найти баг, связанный с темой «prepend», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 44 | prepend — отладка и измерение | Умеет отлаживать «prepend» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «prepend» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «prepend». | Показать, где в DevTools искать проблему по теме «prepend». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 45 | prepend — проектная интеграция | Понимает, как встроить «prepend» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «prepend» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «prepend» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «prepend» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 46 | before — базовое понимание | Понимает назначение и базовое поведение API/механизма «before» в браузере. | Путает назначение «before» или не знает, когда его применять. | Осознанно выбирает «before» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «before» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 47 | before — практическое применение | Умеет применять «before» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «before» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «before» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «before». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 48 | before — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «before». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «before». | Предусматривает ограничения и проектирует безопасное поведение вокруг «before». | Найти баг, связанный с темой «before», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 49 | before — отладка и измерение | Умеет отлаживать «before» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «before» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «before». | Показать, где в DevTools искать проблему по теме «before». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 50 | before — проектная интеграция | Понимает, как встроить «before» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «before» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «before» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «before» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 51 | after — базовое понимание | Понимает назначение и базовое поведение API/механизма «after» в браузере. | Путает назначение «after» или не знает, когда его применять. | Осознанно выбирает «after» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «after» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 52 | after — практическое применение | Умеет применять «after» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «after» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «after» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «after». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 53 | after — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «after». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «after». | Предусматривает ограничения и проектирует безопасное поведение вокруг «after». | Найти баг, связанный с темой «after», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 54 | after — отладка и измерение | Умеет отлаживать «after» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «after» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «after». | Показать, где в DevTools искать проблему по теме «after». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 55 | after — проектная интеграция | Понимает, как встроить «after» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «after» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «after» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «after» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 56 | replaceWith — базовое понимание | Понимает назначение и базовое поведение API/механизма «replaceWith» в браузере. | Путает назначение «replaceWith» или не знает, когда его применять. | Осознанно выбирает «replaceWith» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «replaceWith» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 57 | replaceWith — практическое применение | Умеет применять «replaceWith» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «replaceWith» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «replaceWith» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «replaceWith». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 58 | replaceWith — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «replaceWith». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «replaceWith». | Предусматривает ограничения и проектирует безопасное поведение вокруг «replaceWith». | Найти баг, связанный с темой «replaceWith», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 59 | replaceWith — отладка и измерение | Умеет отлаживать «replaceWith» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «replaceWith» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «replaceWith». | Показать, где в DevTools искать проблему по теме «replaceWith». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 60 | replaceWith — проектная интеграция | Понимает, как встроить «replaceWith» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «replaceWith» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «replaceWith» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «replaceWith» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 61 | remove — базовое понимание | Понимает назначение и базовое поведение API/механизма «remove» в браузере. | Путает назначение «remove» или не знает, когда его применять. | Осознанно выбирает «remove» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «remove» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 62 | remove — практическое применение | Умеет применять «remove» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «remove» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «remove» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «remove». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 63 | remove — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «remove». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «remove». | Предусматривает ограничения и проектирует безопасное поведение вокруг «remove». | Найти баг, связанный с темой «remove», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 64 | remove — отладка и измерение | Умеет отлаживать «remove» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «remove» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «remove». | Показать, где в DevTools искать проблему по теме «remove». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 65 | remove — проектная интеграция | Понимает, как встроить «remove» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «remove» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «remove» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «remove» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 66 | cloneNode — базовое понимание | Понимает назначение и базовое поведение API/механизма «cloneNode» в браузере. | Путает назначение «cloneNode» или не знает, когда его применять. | Осознанно выбирает «cloneNode» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «cloneNode» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 67 | cloneNode — практическое применение | Умеет применять «cloneNode» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «cloneNode» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «cloneNode» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «cloneNode». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 68 | cloneNode — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «cloneNode». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «cloneNode». | Предусматривает ограничения и проектирует безопасное поведение вокруг «cloneNode». | Найти баг, связанный с темой «cloneNode», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 69 | cloneNode — отладка и измерение | Умеет отлаживать «cloneNode» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «cloneNode» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «cloneNode». | Показать, где в DevTools искать проблему по теме «cloneNode». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 70 | cloneNode — проектная интеграция | Понимает, как встроить «cloneNode» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «cloneNode» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «cloneNode» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «cloneNode» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 71 | DocumentFragment — базовое понимание | Понимает назначение и базовое поведение API/механизма «DocumentFragment» в браузере. | Путает назначение «DocumentFragment» или не знает, когда его применять. | Осознанно выбирает «DocumentFragment» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «DocumentFragment» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 72 | DocumentFragment — практическое применение | Умеет применять «DocumentFragment» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «DocumentFragment» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «DocumentFragment» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «DocumentFragment». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 73 | DocumentFragment — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «DocumentFragment». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «DocumentFragment». | Предусматривает ограничения и проектирует безопасное поведение вокруг «DocumentFragment». | Найти баг, связанный с темой «DocumentFragment», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 74 | DocumentFragment — отладка и измерение | Умеет отлаживать «DocumentFragment» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «DocumentFragment» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «DocumentFragment». | Показать, где в DevTools искать проблему по теме «DocumentFragment». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 75 | DocumentFragment — проектная интеграция | Понимает, как встроить «DocumentFragment» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «DocumentFragment» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «DocumentFragment» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «DocumentFragment» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 76 | template.content — базовое понимание | Понимает назначение и базовое поведение API/механизма «template.content» в браузере. | Путает назначение «template.content» или не знает, когда его применять. | Осознанно выбирает «template.content» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «template.content» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 77 | template.content — практическое применение | Умеет применять «template.content» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «template.content» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «template.content» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «template.content». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 78 | template.content — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «template.content». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «template.content». | Предусматривает ограничения и проектирует безопасное поведение вокруг «template.content». | Найти баг, связанный с темой «template.content», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 79 | template.content — отладка и измерение | Умеет отлаживать «template.content» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «template.content» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «template.content». | Показать, где в DevTools искать проблему по теме «template.content». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 80 | template.content — проектная интеграция | Понимает, как встроить «template.content» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «template.content» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «template.content» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «template.content» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 81 | classList — базовое понимание | Понимает назначение и базовое поведение API/механизма «classList» в браузере. | Путает назначение «classList» или не знает, когда его применять. | Осознанно выбирает «classList» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «classList» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 82 | classList — практическое применение | Умеет применять «classList» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «classList» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «classList» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «classList». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 83 | classList — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «classList». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «classList». | Предусматривает ограничения и проектирует безопасное поведение вокруг «classList». | Найти баг, связанный с темой «classList», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 84 | classList — отладка и измерение | Умеет отлаживать «classList» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «classList» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «classList». | Показать, где в DevTools искать проблему по теме «classList». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 85 | classList — проектная интеграция | Понимает, как встроить «classList» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «classList» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «classList» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «classList» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 86 | dataset — базовое понимание | Понимает назначение и базовое поведение API/механизма «dataset» в браузере. | Путает назначение «dataset» или не знает, когда его применять. | Осознанно выбирает «dataset» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «dataset» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 87 | dataset — практическое применение | Умеет применять «dataset» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «dataset» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «dataset» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «dataset». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 88 | dataset — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «dataset». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «dataset». | Предусматривает ограничения и проектирует безопасное поведение вокруг «dataset». | Найти баг, связанный с темой «dataset», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 89 | dataset — отладка и измерение | Умеет отлаживать «dataset» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «dataset» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «dataset». | Показать, где в DevTools искать проблему по теме «dataset». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 90 | dataset — проектная интеграция | Понимает, как встроить «dataset» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «dataset» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «dataset» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «dataset» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 91 | style property — базовое понимание | Понимает назначение и базовое поведение API/механизма «style property» в браузере. | Путает назначение «style property» или не знает, когда его применять. | Осознанно выбирает «style property» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «style property» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 92 | style property — практическое применение | Умеет применять «style property» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «style property» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «style property» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «style property». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 93 | style property — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «style property». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «style property». | Предусматривает ограничения и проектирует безопасное поведение вокруг «style property». | Найти баг, связанный с темой «style property», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 94 | style property — отладка и измерение | Умеет отлаживать «style property» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «style property» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «style property». | Показать, где в DevTools искать проблему по теме «style property». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 95 | style property — проектная интеграция | Понимает, как встроить «style property» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «style property» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «style property» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «style property» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 96 | getComputedStyle — базовое понимание | Понимает назначение и базовое поведение API/механизма «getComputedStyle» в браузере. | Путает назначение «getComputedStyle» или не знает, когда его применять. | Осознанно выбирает «getComputedStyle» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «getComputedStyle» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 97 | getComputedStyle — практическое применение | Умеет применять «getComputedStyle» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «getComputedStyle» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «getComputedStyle» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «getComputedStyle». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 98 | getComputedStyle — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «getComputedStyle». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «getComputedStyle». | Предусматривает ограничения и проектирует безопасное поведение вокруг «getComputedStyle». | Найти баг, связанный с темой «getComputedStyle», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 99 | getComputedStyle — отладка и измерение | Умеет отлаживать «getComputedStyle» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «getComputedStyle» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «getComputedStyle». | Показать, где в DevTools искать проблему по теме «getComputedStyle». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 100 | getComputedStyle — проектная интеграция | Понимает, как встроить «getComputedStyle» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «getComputedStyle» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «getComputedStyle» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «getComputedStyle» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 101 | getBoundingClientRect — базовое понимание | Понимает назначение и базовое поведение API/механизма «getBoundingClientRect» в браузере. | Путает назначение «getBoundingClientRect» или не знает, когда его применять. | Осознанно выбирает «getBoundingClientRect» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «getBoundingClientRect» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 102 | getBoundingClientRect — практическое применение | Умеет применять «getBoundingClientRect» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «getBoundingClientRect» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «getBoundingClientRect» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «getBoundingClientRect». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 103 | getBoundingClientRect — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «getBoundingClientRect». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «getBoundingClientRect». | Предусматривает ограничения и проектирует безопасное поведение вокруг «getBoundingClientRect». | Найти баг, связанный с темой «getBoundingClientRect», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 104 | getBoundingClientRect — отладка и измерение | Умеет отлаживать «getBoundingClientRect» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «getBoundingClientRect» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «getBoundingClientRect». | Показать, где в DevTools искать проблему по теме «getBoundingClientRect». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 105 | getBoundingClientRect — проектная интеграция | Понимает, как встроить «getBoundingClientRect» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «getBoundingClientRect» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «getBoundingClientRect» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «getBoundingClientRect» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 106 | scrollIntoView — базовое понимание | Понимает назначение и базовое поведение API/механизма «scrollIntoView» в браузере. | Путает назначение «scrollIntoView» или не знает, когда его применять. | Осознанно выбирает «scrollIntoView» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «scrollIntoView» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 107 | scrollIntoView — практическое применение | Умеет применять «scrollIntoView» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «scrollIntoView» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «scrollIntoView» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «scrollIntoView». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 108 | scrollIntoView — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «scrollIntoView». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «scrollIntoView». | Предусматривает ограничения и проектирует безопасное поведение вокруг «scrollIntoView». | Найти баг, связанный с темой «scrollIntoView», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 109 | scrollIntoView — отладка и измерение | Умеет отлаживать «scrollIntoView» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «scrollIntoView» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «scrollIntoView». | Показать, где в DevTools искать проблему по теме «scrollIntoView». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 110 | scrollIntoView — проектная интеграция | Понимает, как встроить «scrollIntoView» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «scrollIntoView» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «scrollIntoView» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «scrollIntoView» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 111 | focus — базовое понимание | Понимает назначение и базовое поведение API/механизма «focus» в браузере. | Путает назначение «focus» или не знает, когда его применять. | Осознанно выбирает «focus» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «focus» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 112 | focus — практическое применение | Умеет применять «focus» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «focus» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «focus» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «focus». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 113 | focus — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «focus». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «focus». | Предусматривает ограничения и проектирует безопасное поведение вокруг «focus». | Найти баг, связанный с темой «focus», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 114 | focus — отладка и измерение | Умеет отлаживать «focus» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «focus» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «focus». | Показать, где в DevTools искать проблему по теме «focus». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 115 | focus — проектная интеграция | Понимает, как встроить «focus» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «focus» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «focus» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «focus» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 116 | blur — базовое понимание | Понимает назначение и базовое поведение API/механизма «blur» в браузере. | Путает назначение «blur» или не знает, когда его применять. | Осознанно выбирает «blur» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «blur» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 117 | blur — практическое применение | Умеет применять «blur» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «blur» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «blur» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «blur». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 118 | blur — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «blur». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «blur». | Предусматривает ограничения и проектирует безопасное поведение вокруг «blur». | Найти баг, связанный с темой «blur», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 119 | blur — отладка и измерение | Умеет отлаживать «blur» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «blur» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «blur». | Показать, где в DevTools искать проблему по теме «blur». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 120 | blur — проектная интеграция | Понимает, как встроить «blur» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «blur» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «blur» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «blur» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 121 | selection API — базовое понимание | Понимает назначение и базовое поведение API/механизма «selection API» в браузере. | Путает назначение «selection API» или не знает, когда его применять. | Осознанно выбирает «selection API» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «selection API» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 122 | selection API — практическое применение | Умеет применять «selection API» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «selection API» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «selection API» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «selection API». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 123 | selection API — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «selection API». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «selection API». | Предусматривает ограничения и проектирует безопасное поведение вокруг «selection API». | Найти баг, связанный с темой «selection API», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 124 | selection API — отладка и измерение | Умеет отлаживать «selection API» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «selection API» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «selection API». | Показать, где в DevTools искать проблему по теме «selection API». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 125 | selection API — проектная интеграция | Понимает, как встроить «selection API» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «selection API» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «selection API» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «selection API» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 126 | Range API — базовое понимание | Понимает назначение и базовое поведение API/механизма «Range API» в браузере. | Путает назначение «Range API» или не знает, когда его применять. | Осознанно выбирает «Range API» и объясняет, зачем оно нужно в браузерной логике. | Объяснить «Range API» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 127 | Range API — практическое применение | Умеет применять «Range API» в UI-задачах, не создавая хрупкое поведение интерфейса. | Использует «Range API» без учёта жизненного цикла DOM, событий или побочных эффектов. | Использует «Range API» в реальном сценарии и удерживает поведение интерфейса предсказуемым. | Реализовать UI-кейс, где ключевой механизм — «Range API». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 128 | Range API — ошибки и edge cases | Знает ограничения, кросс-браузерные нюансы и типичные ошибки вокруг «Range API». | Не учитывает крайние случаи, жизненный цикл документа или ограничения браузера для «Range API». | Предусматривает ограничения и проектирует безопасное поведение вокруг «Range API». | Найти баг, связанный с темой «Range API», и исправить его. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 129 | Range API — отладка и измерение | Умеет отлаживать «Range API» через DevTools и измерять влияние на сеть, рендер или память. | Не умеет проверить эффект «Range API» в DevTools и не подтверждает гипотезы измерением. | Подтверждает гипотезы измерением, а не предположениями, и корректно отлаживает «Range API». | Показать, где в DevTools искать проблему по теме «Range API». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 130 | Range API — проектная интеграция | Понимает, как встроить «Range API» в архитектуру проекта, не создавая лишней связанности или утечек. | Встраивает «Range API» точечно и создаёт технический долг, скрытые зависимости или утечки. | Использует «Range API» как часть устойчивой архитектуры модуля или фичи. | Предложить guideline по применению «Range API» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |

---

[← К разделу DOM и Browser](./README.md)

---
id: "ui-6.2"
domain: "ui-frameworks"
level: "junior"
title: "React: hooks и жизненный цикл"
prerequisites:
  - "ui-6.1"
skills:
  - "reactivity-models"
  - "update-cycle"
  - "effects-boundaries"
  - "react-implementation"
outcomes:
  - "разобрать именно React-реализацию этой темы без смешения с другими framework-ами"
  - "увидеть React API, ограничения и типичные ошибки на одном маршруте обучения"
  - "использовать общий core-слой как базу, а не как встроенный кусок внутри React-урока"
tags:
  - "ui-frameworks"
  - "reactivity"
  - "core"
  - "react"
practice_level: "junior"
---

# 6.2 React: hooks и жизненный цикл

Это отдельный React-слой темы. Общую модель и сравнение подходов смотри в [Update cycle и реактивность UI framework-ов](../core/6.2-update-cycle-и-реактивность-ui-framework-ов.md).

---

## Что уже должно быть знакомо

- общая модель темы: [Update cycle и реактивность UI framework-ов](../core/6.2-update-cycle-и-реактивность-ui-framework-ов.md)
- предыдущий React-слой: [React: базовые принципы и компонентная модель](../../10-intern/react/6.1-react-базовые-принципы-и-компонентная-модель.md)

---

## 🎯 Что ты освоишь

- разобрать именно React-реализацию этой темы без смешения с другими framework-ами;
- увидеть React API, ограничения и типичные ошибки на одном маршруте обучения;
- использовать общий core-слой как базу, а не как встроенный кусок внутри React-урока.

---

## Термины урока

- `Render` — это вычисление следующего UI на основе текущих входов и состояния.
- `Commit` — это применение рассчитанных изменений к DOM или другому target runtime.
- `Update cycle` — это полный путь от изменения данных до нового состояния интерфейса.
- `Derived state` — это значение, которое можно вычислить из уже существующих данных без отдельного хранения.
- `Side effect` — это синхронизация с внешней системой: сетью, DOM API, таймером, подпиской, storage.
- `Explicit dependencies` — это модель, где разработчик явно задает, от чего зависит вычисление или эффект.

---

## Реализация в React

### Rules of hooks

Hooks в React должны вызываться одинаково на каждом рендере. Это техническое следствие того, как React связывает состояние вызовов с позицией hook в компоненте.

### Какие hooks junior реально должен понимать

- `useState` для локального UI state;
- `useRef` для значений без ререндера и доступа к DOM;
- `useEffect` для синхронизации с внешней системой;
- `useReducer` для более явных переходов состояния;
- `useContext` для передачи данных через дерево;
- custom hooks для переиспользуемой hook-логики.

### `useEffect`: где он нужен, а где нет

`useEffect` нужен, когда есть сеть, подписка, таймер, imperative DOM API, `localStorage`, socket или другая внешняя система.

Он не нужен, когда:

- можно посчитать derived value во время render;
- можно обработать событие прямо в handler;
- значение можно получить из `props` и текущего `state` без дополнительного хранения.

### Stale closures и cleanup

`Stale closure` — это не "магия React", а обычное следствие JS-замыканий. Если effect или callback держит старое значение, он будет работать со старым снимком данных.

`Cleanup` нужен, чтобы:

- снять подписку;
- отменить таймер;
- отцепить listener;
- не оставить внешнюю систему в несогласованном состоянии.

### Custom hooks

Custom hook полезен, когда повторяется не кусок JSX, а именно поведенческий сценарий:

- загрузка и синхронизация данных;
- работа со scroll или media query;
- form flow;
- reusable toggle/filter/search behavior.

---

## Частые ошибки

- используют `useEffect` для вычисления derived state;
- не различают render-логику и side effects;
- забывают cleanup для подписок, таймеров и запросов;
- выносят в custom hook случайный набор строк, а не повторяемую поведенческую модель;
- учат hooks как API-список, а не как способ описать update cycle.

---

## Anti-pattern

"Любое изменение данных нужно отражать через `useEffect`".

Нет. Effect — это не универсальный клей. Если значения можно честно получить из текущего render-контекста, effect только усложнит модель.

---

## 🔧 Практика

1. Возьми компонент с несколькими hooks и для каждого вызова скажи: это local state, ref, effect, reducer или reusable behavior.
1. Найди в проекте один `useEffect`, который на самом деле можно заменить на derived value во время render.
1. Найди один effect, где реально нужен cleanup, и объясни, что сломается без него.
1. Сравни мысленно этот сценарий с Vue-подходом: где здесь был бы `computed`, а где `watch`.

---

## ✅ Чеклист

- [ ] Я понимаю разницу между render, commit и side effect
- [ ] Я различаю derived state и эффект
- [ ] Я знаю, когда `useEffect` нужен, а когда лишний
- [ ] Я понимаю, откуда берутся stale closures
- [ ] Я вижу разницу между explicit dependencies и reactive tracking

---

## Следующий шаг развития

- Следующий React-слой: [React Router: routing architecture](6.3-react-router-и-routing-architecture.md)

---

## 📚 Полезные материалы

- [React: Synchronizing with Effects](https://react.dev/learn/synchronizing-with-effects)
- [React: You Might Not Need an Effect](https://react.dev/learn/you-might-not-need-an-effect)


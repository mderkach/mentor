---
id: "ui-6.7"
domain: "ui-frameworks"
level: "middle"
title: "React: state management"
prerequisites:
  - "ui-6.6"
skills:
  - "state-boundaries"
  - "store-selection"
  - "reactive-vs-event-state"
  - "react-implementation"
outcomes:
  - "разобрать именно React-реализацию этой темы без смешения с другими framework-ами"
  - "увидеть React API, ограничения и типичные ошибки на одном маршруте обучения"
  - "использовать общий core-слой как базу, а не как встроенный кусок внутри React-урока"
tags:
  - "ui-frameworks"
  - "state-management"
  - "architecture"
  - "react"
practice_level: "middle"
---

# 6.7 React: state management

Это отдельный React-слой темы. Общую модель и сравнение подходов смотри в [Модели управления состоянием](../core/6.7-модели-управления-состоянием.md).

---

## Что уже должно быть знакомо

- общая модель темы: [Модели управления состоянием](../core/6.7-модели-управления-состоянием.md)
- предыдущий React-слой: [React: server state и data fetching](6.6-react-data-fetching-и-server-state.md)

---

## 🎯 Что ты освоишь

- разобрать именно React-реализацию этой темы без смешения с другими framework-ами;
- увидеть React API, ограничения и типичные ошибки на одном маршруте обучения;
- использовать общий core-слой как базу, а не как встроенный кусок внутри React-урока.

---

## Термины урока

- `Local UI state` — это состояние конкретного компонента или близкого поддерева.
- `Shared UI state` — это состояние, которое нужно нескольким связанным частям UI.
- `URL state` — это навигационное состояние, которое должно быть shareable через ссылку.
- `Store` — это отдельный контейнер состояния, живущий дольше одного компонента.
- `Selector` — это функция, которая выбирает и вычисляет нужный кусок состояния.
- `State machine` — это модель, где возможные состояния и переходы между ними описаны явно.

---

## Реализация в React

В React-экосистеме выбор обычно идет по градации сложности:

- `Context` — для узкого shared contract;
- `Redux Toolkit` — когда нужны явные события, selectors, normalized state и инструменты;
- `Zustand` — для простого shared state без большого ceremony;
- `MobX` — когда команда сознательно выбирает reactive object model;
- `XState` и другие state machine tools — когда workflow-сценарий важнее "обычного state".

Что важно удерживать:

- `server state` не надо тащить в client store по привычке;
- selectors и normalized state нужны не "потому что Redux", а потому что они делают store устойчивым;
- store не должен превращаться в глобальный мешок всех значений приложения.

---

## Частые ошибки

- заводят store "на старте, потому что взрослый проект";
- смешивают client state, URL state и server state в один контейнер;
- подписываются на весь store вместо адресных selectors/subscriptions;
- дублируют derived values вместо вычисления из source of truth;
- пытаются починить хаотичный workflow еще одним флагом вместо state machine.

---

## Anti-pattern

"Если состояние сложное, его надо вынести в store".

Не всегда. Иногда проблема не в размере состояния, а в том, что команда не развела его по типам и границам.

---

## 🔧 Практика

1. Возьми один сложный экран и классифицируй его состояние по типам: local, shared UI, URL, server, ephemeral.
1. Для каждого типа объясни, почему он должен жить именно там, а не в store по умолчанию.
1. Выбери между `Context`, `Redux Toolkit`, `Zustand`, `MobX` и state machine для одного сценария и аргументируй выбор.
1. Найди одно место, где selectors или normalized state уберут лишнюю связанность.

---

## ✅ Чеклист

- [ ] Я различаю типы состояния в UI-приложении
- [ ] Я не завожу store без явной причины
- [ ] Я понимаю разницу между event-driven и reactive state моделями
- [ ] Я знаю, зачем нужны selectors и normalized state
- [ ] Я не смешиваю server state с client store по привычке

---

## Следующий шаг развития

- Следующий React-слой: [React: оптимизация приложений](../../40-senior/react/6.8-react-оптимизация-приложений.md)

---

## 📚 Полезные материалы

- [Redux Style Guide](https://redux.js.org/style-guide/)
- [Zustand Docs](https://zustand.docs.pmnd.rs/getting-started/introduction)
- [MobX Docs](https://mobx.js.org/README.html)
- [Pinia Docs](https://pinia.vuejs.org/)

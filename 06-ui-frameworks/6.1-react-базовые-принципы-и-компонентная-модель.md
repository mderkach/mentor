# 6.1 React: базовые принципы и компонентная модель

- Домен: **UI Frameworks**
- Атомарных навыков: **70**
- Тип: **Hard**
- Технология: **React**
- Описание технологии: UI-библиотека для построения компонентных интерфейсов.

## Навыки

| # | Навык | Описание | Индикатор развития | Критерии успеха | Проверка | Инструмент развития |
|---|---|---|---|---|---|---|
| 1 | JSX — базовое понимание | Понимает, какую роль «JSX» играет в современном UI-фреймворке и React-экосистеме. | Использует «JSX» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «JSX» и понимает его место в компонентной модели. | Кратко объяснить «JSX» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 2 | JSX — практическое применение | Умеет применять «JSX» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «JSX» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «JSX» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «JSX». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 3 | JSX — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «JSX». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «JSX». | Предвидит типичные ошибки вокруг «JSX» и закладывает безопасное поведение. | Найти и исправить баг по теме «JSX». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 4 | JSX — производительность и DX | Понимает влияние «JSX» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «JSX» ухудшает производительность, тестируемость или DX проекта. | Использует «JSX» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «JSX». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 5 | JSX — проектная интеграция | Умеет встроить «JSX» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «JSX» точечно без общих правил, создавая неоднородный код. | Интегрирует «JSX» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «JSX» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 6 | props — базовое понимание | Понимает, какую роль «props» играет в современном UI-фреймворке и React-экосистеме. | Использует «props» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «props» и понимает его место в компонентной модели. | Кратко объяснить «props» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 7 | props — практическое применение | Умеет применять «props» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «props» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «props» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «props». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 8 | props — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «props». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «props». | Предвидит типичные ошибки вокруг «props» и закладывает безопасное поведение. | Найти и исправить баг по теме «props». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 9 | props — производительность и DX | Понимает влияние «props» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «props» ухудшает производительность, тестируемость или DX проекта. | Использует «props» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «props». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 10 | props — проектная интеграция | Умеет встроить «props» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «props» точечно без общих правил, создавая неоднородный код. | Интегрирует «props» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «props» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 11 | children — базовое понимание | Понимает, какую роль «children» играет в современном UI-фреймворке и React-экосистеме. | Использует «children» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «children» и понимает его место в компонентной модели. | Кратко объяснить «children» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 12 | children — практическое применение | Умеет применять «children» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «children» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «children» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «children». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 13 | children — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «children». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «children». | Предвидит типичные ошибки вокруг «children» и закладывает безопасное поведение. | Найти и исправить баг по теме «children». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 14 | children — производительность и DX | Понимает влияние «children» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «children» ухудшает производительность, тестируемость или DX проекта. | Использует «children» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «children». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 15 | children — проектная интеграция | Умеет встроить «children» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «children» точечно без общих правил, создавая неоднородный код. | Интегрирует «children» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «children» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 16 | key — базовое понимание | Понимает, какую роль «key» играет в современном UI-фреймворке и React-экосистеме. | Использует «key» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «key» и понимает его место в компонентной модели. | Кратко объяснить «key» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 17 | key — практическое применение | Умеет применять «key» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «key» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «key» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «key». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 18 | key — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «key». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «key». | Предвидит типичные ошибки вокруг «key» и закладывает безопасное поведение. | Найти и исправить баг по теме «key». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 19 | key — производительность и DX | Понимает влияние «key» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «key» ухудшает производительность, тестируемость или DX проекта. | Использует «key» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «key». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 20 | key — проектная интеграция | Умеет встроить «key» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «key» точечно без общих правил, создавая неоднородный код. | Интегрирует «key» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «key» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 21 | controlled components — базовое понимание | Понимает, какую роль «controlled components» играет в современном UI-фреймворке и React-экосистеме. | Использует «controlled components» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «controlled components» и понимает его место в компонентной модели. | Кратко объяснить «controlled components» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 22 | controlled components — практическое применение | Умеет применять «controlled components» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «controlled components» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «controlled components» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «controlled components». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 23 | controlled components — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «controlled components». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «controlled components». | Предвидит типичные ошибки вокруг «controlled components» и закладывает безопасное поведение. | Найти и исправить баг по теме «controlled components». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 24 | controlled components — производительность и DX | Понимает влияние «controlled components» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «controlled components» ухудшает производительность, тестируемость или DX проекта. | Использует «controlled components» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «controlled components». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 25 | controlled components — проектная интеграция | Умеет встроить «controlled components» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «controlled components» точечно без общих правил, создавая неоднородный код. | Интегрирует «controlled components» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «controlled components» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 26 | uncontrolled components — базовое понимание | Понимает, какую роль «uncontrolled components» играет в современном UI-фреймворке и React-экосистеме. | Использует «uncontrolled components» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «uncontrolled components» и понимает его место в компонентной модели. | Кратко объяснить «uncontrolled components» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 27 | uncontrolled components — практическое применение | Умеет применять «uncontrolled components» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «uncontrolled components» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «uncontrolled components» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «uncontrolled components». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 28 | uncontrolled components — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «uncontrolled components». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «uncontrolled components». | Предвидит типичные ошибки вокруг «uncontrolled components» и закладывает безопасное поведение. | Найти и исправить баг по теме «uncontrolled components». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 29 | uncontrolled components — производительность и DX | Понимает влияние «uncontrolled components» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «uncontrolled components» ухудшает производительность, тестируемость или DX проекта. | Использует «uncontrolled components» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «uncontrolled components». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 30 | uncontrolled components — проектная интеграция | Умеет встроить «uncontrolled components» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «uncontrolled components» точечно без общих правил, создавая неоднородный код. | Интегрирует «uncontrolled components» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «uncontrolled components» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 31 | lifting state up — базовое понимание | Понимает, какую роль «lifting state up» играет в современном UI-фреймворке и React-экосистеме. | Использует «lifting state up» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «lifting state up» и понимает его место в компонентной модели. | Кратко объяснить «lifting state up» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 32 | lifting state up — практическое применение | Умеет применять «lifting state up» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «lifting state up» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «lifting state up» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «lifting state up». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 33 | lifting state up — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «lifting state up». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «lifting state up». | Предвидит типичные ошибки вокруг «lifting state up» и закладывает безопасное поведение. | Найти и исправить баг по теме «lifting state up». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 34 | lifting state up — производительность и DX | Понимает влияние «lifting state up» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «lifting state up» ухудшает производительность, тестируемость или DX проекта. | Использует «lifting state up» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «lifting state up». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 35 | lifting state up — проектная интеграция | Умеет встроить «lifting state up» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «lifting state up» точечно без общих правил, создавая неоднородный код. | Интегрирует «lifting state up» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «lifting state up» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 36 | composition over inheritance — базовое понимание | Понимает, какую роль «composition over inheritance» играет в современном UI-фреймворке и React-экосистеме. | Использует «composition over inheritance» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «composition over inheritance» и понимает его место в компонентной модели. | Кратко объяснить «composition over inheritance» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 37 | composition over inheritance — практическое применение | Умеет применять «composition over inheritance» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «composition over inheritance» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «composition over inheritance» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «composition over inheritance». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 38 | composition over inheritance — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «composition over inheritance». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «composition over inheritance». | Предвидит типичные ошибки вокруг «composition over inheritance» и закладывает безопасное поведение. | Найти и исправить баг по теме «composition over inheritance». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 39 | composition over inheritance — производительность и DX | Понимает влияние «composition over inheritance» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «composition over inheritance» ухудшает производительность, тестируемость или DX проекта. | Использует «composition over inheritance» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «composition over inheritance». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 40 | composition over inheritance — проектная интеграция | Умеет встроить «composition over inheritance» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «composition over inheritance» точечно без общих правил, создавая неоднородный код. | Интегрирует «composition over inheritance» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «composition over inheritance» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 41 | event handling in React — базовое понимание | Понимает, какую роль «event handling in React» играет в современном UI-фреймворке и React-экосистеме. | Использует «event handling in React» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «event handling in React» и понимает его место в компонентной модели. | Кратко объяснить «event handling in React» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 42 | event handling in React — практическое применение | Умеет применять «event handling in React» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «event handling in React» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «event handling in React» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «event handling in React». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 43 | event handling in React — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «event handling in React». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «event handling in React». | Предвидит типичные ошибки вокруг «event handling in React» и закладывает безопасное поведение. | Найти и исправить баг по теме «event handling in React». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 44 | event handling in React — производительность и DX | Понимает влияние «event handling in React» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «event handling in React» ухудшает производительность, тестируемость или DX проекта. | Использует «event handling in React» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «event handling in React». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 45 | event handling in React — проектная интеграция | Умеет встроить «event handling in React» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «event handling in React» точечно без общих правил, создавая неоднородный код. | Интегрирует «event handling in React» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «event handling in React» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 46 | synthetic events — базовое понимание | Понимает, какую роль «synthetic events» играет в современном UI-фреймворке и React-экосистеме. | Использует «synthetic events» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «synthetic events» и понимает его место в компонентной модели. | Кратко объяснить «synthetic events» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 47 | synthetic events — практическое применение | Умеет применять «synthetic events» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «synthetic events» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «synthetic events» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «synthetic events». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 48 | synthetic events — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «synthetic events». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «synthetic events». | Предвидит типичные ошибки вокруг «synthetic events» и закладывает безопасное поведение. | Найти и исправить баг по теме «synthetic events». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 49 | synthetic events — производительность и DX | Понимает влияние «synthetic events» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «synthetic events» ухудшает производительность, тестируемость или DX проекта. | Использует «synthetic events» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «synthetic events». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 50 | synthetic events — проектная интеграция | Умеет встроить «synthetic events» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «synthetic events» точечно без общих правил, создавая неоднородный код. | Интегрирует «synthetic events» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «synthetic events» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 51 | refs — базовое понимание | Понимает, какую роль «refs» играет в современном UI-фреймворке и React-экосистеме. | Использует «refs» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «refs» и понимает его место в компонентной модели. | Кратко объяснить «refs» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 52 | refs — практическое применение | Умеет применять «refs» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «refs» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «refs» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «refs». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 53 | refs — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «refs». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «refs». | Предвидит типичные ошибки вокруг «refs» и закладывает безопасное поведение. | Найти и исправить баг по теме «refs». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 54 | refs — производительность и DX | Понимает влияние «refs» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «refs» ухудшает производительность, тестируемость или DX проекта. | Использует «refs» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «refs». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 55 | refs — проектная интеграция | Умеет встроить «refs» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «refs» точечно без общих правил, создавая неоднородный код. | Интегрирует «refs» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «refs» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 56 | forwardRef — базовое понимание | Понимает, какую роль «forwardRef» играет в современном UI-фреймворке и React-экосистеме. | Использует «forwardRef» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «forwardRef» и понимает его место в компонентной модели. | Кратко объяснить «forwardRef» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 57 | forwardRef — практическое применение | Умеет применять «forwardRef» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «forwardRef» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «forwardRef» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «forwardRef». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 58 | forwardRef — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «forwardRef». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «forwardRef». | Предвидит типичные ошибки вокруг «forwardRef» и закладывает безопасное поведение. | Найти и исправить баг по теме «forwardRef». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 59 | forwardRef — производительность и DX | Понимает влияние «forwardRef» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «forwardRef» ухудшает производительность, тестируемость или DX проекта. | Использует «forwardRef» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «forwardRef». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 60 | forwardRef — проектная интеграция | Умеет встроить «forwardRef» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «forwardRef» точечно без общих правил, создавая неоднородный код. | Интегрирует «forwardRef» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «forwardRef» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 61 | React DevTools — базовое понимание | Понимает, какую роль «React DevTools» играет в современном UI-фреймворке и React-экосистеме. | Использует «React DevTools» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «React DevTools» и понимает его место в компонентной модели. | Кратко объяснить «React DevTools» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 62 | React DevTools — практическое применение | Умеет применять «React DevTools» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «React DevTools» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «React DevTools» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «React DevTools». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 63 | React DevTools — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «React DevTools». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «React DevTools». | Предвидит типичные ошибки вокруг «React DevTools» и закладывает безопасное поведение. | Найти и исправить баг по теме «React DevTools». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 64 | React DevTools — производительность и DX | Понимает влияние «React DevTools» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «React DevTools» ухудшает производительность, тестируемость или DX проекта. | Использует «React DevTools» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «React DevTools». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 65 | React DevTools — проектная интеграция | Умеет встроить «React DevTools» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «React DevTools» точечно без общих правил, создавая неоднородный код. | Интегрирует «React DevTools» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «React DevTools» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 66 | StrictMode — базовое понимание | Понимает, какую роль «StrictMode» играет в современном UI-фреймворке и React-экосистеме. | Использует «StrictMode» шаблонно, не понимая его места в архитектуре интерфейса. | Осознанно выбирает «StrictMode» и понимает его место в компонентной модели. | Кратко объяснить «StrictMode» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 67 | StrictMode — практическое применение | Умеет применять «StrictMode» в реальных интерфейсах с учётом читаемости, повторного использования и UX. | Применяет «StrictMode» формально и создаёт хрупкие компоненты, эффекты или связи между модулями. | Применяет «StrictMode» так, чтобы решение оставалось читаемым и устойчивым к развитию. | Решить задачу на UI с опорой на «StrictMode». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 68 | StrictMode — ошибки и edge cases | Знает крайние случаи, ловушки и побочные эффекты, связанные с «StrictMode». | Не учитывает замыкания, лишние рендеры, гонки данных или ограничения инструмента «StrictMode». | Предвидит типичные ошибки вокруг «StrictMode» и закладывает безопасное поведение. | Найти и исправить баг по теме «StrictMode». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 69 | StrictMode — производительность и DX | Понимает влияние «StrictMode» на рендер, сеть, кэш, DX команды и тестируемость. | Не замечает, как «StrictMode» ухудшает производительность, тестируемость или DX проекта. | Использует «StrictMode» с учётом производительности, DX и поддержки команды. | Показать, как измерить или улучшить работу решения, связанного с «StrictMode». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 70 | StrictMode — проектная интеграция | Умеет встроить «StrictMode» в кодовую базу: через архитектуру модулей, conventions и tooling. | Использует «StrictMode» точечно без общих правил, создавая неоднородный код. | Интегрирует «StrictMode» в архитектуру проекта и поддерживает единый engineering style. | Предложить правило/гайдлайн по использованию «StrictMode» в проекте. | Дока / практика / код-ревью / мини-проект / документация инструмента. |

---

[← К разделу UI Frameworks](./README.md)

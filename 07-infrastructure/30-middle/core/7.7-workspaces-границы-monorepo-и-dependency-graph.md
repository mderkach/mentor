---
id: "infra-core-7.7"
domain: "infrastructure"
level: "middle"
title: "Workspaces, границы monorepo и dependency graph"
prerequisites:
  - "infra-core-7.6"
skills:
  - "dependency-governance"
  - "semver"
  - "dependency-risk"
  - "workspaces"
  - "dependency-graph"
  - "package-boundaries"
outcomes:
  - "зачем monorepo вообще может быть нужен"
  - "что такое workspace как organizational boundary"
  - "как читать dependency graph как карту связности"
  - "где shared code превращается в shared coupling"
tags:
  - "infrastructure"
  - "dependencies"
  - "governance"
  - "monorepo"
  - "workspaces"
practice_level: "middle"
---

# 7.7 Workspaces, границы monorepo и dependency graph

Как только в проекте появляются несколько приложений и shared packages, разговор быстро уезжает к `Nx` или `Turborepo`. Ошибка в том, что до orchestration нужно собрать базу: зачем вообще нужен monorepo, где package boundaries и что dependency graph говорит о связности системы.

---

## Что уже должно быть знакомо

- [Code splitting, source maps и диагностика сборки](7.6-code-splitting-source-maps-и-диагностика-сборки.md)

---

## 🎯 Что ты освоишь

- зачем monorepo вообще может быть нужен;
- что такое workspace как organizational boundary;
- как читать dependency graph как карту связности;
- где shared code превращается в shared coupling.

---

## Термины урока

- `Workspace` — это механизм управления несколькими пакетами в одном репозитории.
- `Monorepo` — это подход, при котором несколько приложений и библиотек живут в одной кодовой базе.
- `Dependency graph` — это карта связей между пакетами и задачами.
- `Package boundary` — это явно ограниченный public API пакета и его зона ответственности.
- `Shared coupling` — это скрытая связанность через "общий" код без ясного владельца и контракта.

---

## Core / Fundamentals

Monorepo нужен не потому, что так делают большие компании. Он нужен, когда у команды реально есть:

- несколько приложений или пакетов;
- общий tooling contract;
- shared code с понятной ценностью;
- потребность видеть связность изменений.

Но вместе с этим появляется цена:

- сложнее держать явные зоны ответственности;
- выше риск shared coupling;
- одна грязная boundary быстрее заражает весь граф;
- package boundaries становятся инфраструктурной, а не только кодовой темой.

Dependency graph нужен не для красивой картинки, а чтобы понимать:

- что реально затрагивает изменение;
- где пакет стал слишком связанным;
- какие границы уже не держатся.

---

## Границы применимости инструмента

- workspaces полезны, когда реально есть пакетные границы, а не просто много папок;
- monorepo окупается, если shared code и общий tooling дают больше пользы, чем coordination cost;
- shared package без owner'а быстро становится источником боли;
- workspaces не заменяют ни архитектурные границы, ни orchestration tooling.

---

## Альтернативы кратко

- `pnpm`, `npm`, `yarn` workspaces решают одну базовую задачу, но отличаются DX и dependency handling.
- `Nx` и `Turborepo` нужно мыслить как следующий слой orchestration, а не как способ "внезапно сделать monorepo зрелым".

---

## Production failure modes

- monorepo создается раньше, чем возникла реальная межпакетная проблема;
- внутренности пакетов импортируются напрямую, а public API игнорируется;
- dependency graph грязный, но команда этого не видит;
- shared package тащится всеми, но никто им не владеет.

---

## Anti-pattern

"Сделаем monorepo, и порядок появится сам".

Чаще происходит обратное: плохие границы становятся общими быстрее и дешевле.

---

## 🔧 Практика

1. Возьми проект с несколькими пакетами и нарисуй его dependency graph в упрощенном виде.
1. Для каждого shared package назови owner'а и public API.
1. Найди одно место, где shared code уже стал shared coupling.
1. Аргументируй, monorepo здесь реально окупается или пока создает ложное чувство порядка.

---

## ✅ Чеклист

- [ ] Я понимаю, зачем monorepo может быть нужен
- [ ] Я различаю workspace mechanic и package boundary
- [ ] Я умею читать dependency graph как карту связности
- [ ] Я вижу риск shared package без owner'а
- [ ] Я не считаю workspaces автоматическим признаком зрелости

---

## Следующий шаг развития

- Конкретная реализация: [Workspaces: пакетные границы и dependency graph](../workspaces-monorepo/7.7-workspaces-и-dependency-graph.md)

---

## 📚 Полезные материалы

- [pnpm Workspaces](https://pnpm.io/workspaces)
- [npm Workspaces](https://docs.npmjs.com/cli/using-npm/workspaces)
- [Yarn Workspaces](https://yarnpkg.com/features/workspaces)


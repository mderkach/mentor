---
id: "infra-core-7.4"
domain: "infrastructure"
level: "junior"
title: "Build systems и env"
prerequisites:
  - "infra-core-7.3"
skills:
  - "build-systems"
  - "env-handling"
  - "build-config"
outcomes:
  - "что такое build pipeline frontend-приложения"
  - "чем отличаются dev server, transform, bundling и production build"
  - "где проходит граница между public env, runtime config и секретами"
  - "как это выражается в Vite и где нужны другие build-стратегии"
tags:
  - "infrastructure"
  - "build"
  - "tooling"
practice_level: "junior"
---

# 7.4 Build systems и env

Этот урок нужно читать не как "введение в Vite", а как введение в frontend build systems. `Vite` здесь — основной пример реализации, но не сама суть.

Если это не различать, команда начинает путать инженерную проблему со знакомым инструментом.

---

## Что уже должно быть знакомо

- [Управление зависимостями и обновления без хаоса](7.3-управление-зависимостями-и-обновления-без-хаоса.md)

---

## 🎯 Что ты освоишь

- что такое build pipeline frontend-приложения;
- чем отличаются dev server, transform, bundling и production build;
- где проходит граница между public env, runtime config и секретами;
- как это выражается в Vite и где нужны другие build-стратегии.

---

## Термины урока

- `Build pipeline` — это путь от исходников до deployable artifact.
- `Dev server` — это среда локальной разработки с быстрым feedback loop.
- `Build-time config` — это конфигурация, вшиваемая в артефакт на этапе сборки.
- `Runtime config` — это конфигурация, которая доступна уже после сборки в окружении доставки.
- `Env drift` — это рассинхрон между окружениями по конфигу и поведению.

---

## Core / Fundamentals

Frontend build system решает несколько задач:

- преобразование исходников и модулей;
- локальный dev loop;
- сборку production artifact;
- работу с aliases, assets и source maps;
- инъекцию конфигурации;
- различие между dev и prod behavior.

Особенно важно различать:

- `build-time config` и `runtime config`;
- public env и секреты;
- "локально работает" и "артефакт воспроизводим и deployable".

Во фронтенде `env` — это не просто переменные. Это часть архитектуры поставки: что попадает в клиентский код, что должно жить на сервере, что может разойтись между dev/stage/prod.

---

## Границы применимости инструмента

- `Vite` прекрасен для современного frontend DX, но не является синонимом build system как такового;
- `Vite` не решает автоматически runtime config, cache strategy и delivery architecture;
- если проект требует более специфических интеграций, сложного legacy-bundling или platform-level customization, одной familiarity с Vite может быть мало.

---

## Альтернативы кратко

- `Webpack` остается важным там, где нужна зрелая и очень гибкая plugin ecosystem.
- `Rspack` предлагает похожую экосистемную форму с другим performance profile.
- `esbuild-oriented tooling` делает ставку на скорость и простоту, но не закрывает все сценарии одинаково.
- `Turbopack` пока стоит мыслить как evolving story, а не как зрелый "дефолт мира".

---

## Production failure modes

- секреты попадают в клиентский env;
- aliases синхронизированы в `vite.config`, но забыты в `tsconfig` или тестовой среде;
- локальный dev server ведет себя не так, как production build, и команда это замечает слишком поздно;
- env drift между stage и prod ломает приложение при формально успешной сборке.

---

## Anti-pattern

"Мы используем Vite, значит инфраструктурная часть сборки уже понятна".

Нет. Это значит только то, что у вас есть один конкретный инструмент. Понять нужно саму build-систему и границы ее применимости.

---

## 🔧 Практика

1. Возьми текущий проект и разложи его build pipeline: dev server, transforms, bundling, output artifact, config injection.
1. Назови все env, которые реально уходят в клиент, и объясни, почему там нет секретов.
1. Проверь, синхронизированы ли aliases между build, typecheck и test tooling.
1. Определи один риск env drift между local, stage и prod.

---

## ✅ Чеклист

- [ ] Я понимаю build pipeline шире, чем один инструмент
- [ ] Я различаю dev server и production build
- [ ] Я понимаю границу между public env и секретами
- [ ] Я вижу разницу между build-time и runtime config
- [ ] Я не считаю Vite синонимом всей build architecture

---

## Следующий шаг развития

- Конкретная реализация: [Vite: env и базовая сборка frontend-приложения](../vite/7.4-vite-env-и-базовая-сборка-frontend-приложения.md)

---

## 📚 Полезные материалы

- [Vite Guide](https://vite.dev/guide/)
- [Vite Env and Mode](https://vite.dev/guide/env-and-mode.html)
- [Webpack Concepts](https://webpack.js.org/concepts/)
- [Rspack Guide](https://rspack.dev/guide/start/introduction)


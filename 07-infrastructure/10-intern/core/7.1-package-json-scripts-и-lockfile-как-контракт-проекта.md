---
id: "infra-core-7.1"
domain: "infrastructure"
level: "intern"
title: "`package.json`, scripts и lockfile как контракт проекта"
prerequisites:
  - "infra-core-0.0.1"
skills:
  - "package-json"
  - "operational-contract"
  - "lockfile-discipline"
outcomes:
  - "что такое `package.json` как манифест проекта и зачем он нужен"
  - "как читать `scripts` как рабочий контракт проекта"
  - "чем отличаются `dependencies`, `devDependencies` и `peerDependencies`"
  - "почему lockfile — это не мусорный файл и не побочный эффект install"
tags:
  - "infrastructure"
  - "manifest"
  - "lockfile"
practice_level: "intern"
---

# 7.1 `package.json`, scripts и lockfile как контракт проекта

`package.json` — это не просто список пакетов. Это манифест проекта и интерфейс команды с проектом: что запускать, чем он живет, какие есть сценарии сборки и проверки.

`Lockfile` рядом нужен для другой задачи: зафиксировать точное дерево зависимостей и сделать установку воспроизводимой.

---

## Что уже должно быть знакомо

- [Рабочее окружение frontend-проекта](../../00-zero/core/0.0.1-рабочее-окружение-frontend-проекта.md)

---

## 🎯 Что ты освоишь

- что такое `package.json` как манифест проекта и зачем он нужен;
- как читать `scripts` как рабочий контракт проекта;
- чем отличаются `dependencies`, `devDependencies` и `peerDependencies`;
- почему lockfile — это не мусорный файл и не побочный эффект install.

---

## Термины урока

- `Манифест` — это файл, который описывает метаданные проекта, зависимости и его интерфейс запуска.
- `Script` — это именованная команда проекта, запускаемая через package manager.
- `Lockfile` — это файл с точными версиями и графом зависимостей.
- `Peer dependency` — это зависимость, которую пакет ожидает получить из окружения проекта.

---

## Core / Fundamentals

У `package.json` есть несколько разных ролей:

- описать проект как пакет;
- зафиксировать зависимости и разделение runtime/dev/peer;
- дать команде точку входа в типовые сценарии;
- задать рабочий контракт для локальной работы и CI.

У `lockfile` роль другая:

- сделать установку повторяемой;
- зафиксировать фактическое дерево зависимостей;
- позволить CI проверить, что никто не пересобрал dependency graph случайным образом.

Поэтому относиться к `scripts` как к мусорному ящику нельзя. Если проект реально требует `lint`, `test`, `build` или `typecheck`, это должно быть видно в манифесте, а не жить в памяти тимлида.

---

## Границы применимости инструмента

- scripts удобны как интерфейс команды, но не заменяют полноценный task orchestration на масштабе monorepo;
- `npx`/`pnpm dlx` хороши для одноразовых CLI, но не должны подменять project-level tooling contract;
- lockfile полезен только если команда признает один manager и один источник истины.

---

## Альтернативы кратко

- `npm`, `pnpm`, `yarn` дают общий интерфейс scripts, но отличаются по workspace behavior и управлению зависимостями.
- Для сложных task chains иногда приходят task runners и orchestration tools, но manifest все равно остается точкой входа.

---

## Production failure modes

- в репозитории живет несколько lockfile, и никто уже не понимает, какой из них основной;
- важные команды существуют в wiki, но не в scripts;
- `peerDependencies` игнорируются, пока не ломается build или runtime;
- script chaining превращается в нечитаемый набор shell-хаков, который никто не умеет поддерживать.

---

## Anti-pattern

"Я просто знаю, что надо писать `npm run dev`, и этого достаточно".

Нет. Нужно понимать, что это за контракт, где он описан и какие еще команды проект считает обязательными.

---

## 🔧 Практика

1. Открой `package.json` любого проекта и объясни каждую dependency section.
1. Назови минимальный локальный цикл проверок, который должен быть виден в `scripts`.
1. Найди основной lockfile и объясни, почему другой package manager в этом проекте будет проблемой.
1. Определи один script, который сейчас выглядит как мусорный ящик, и предложи, как сделать его понятнее.

---

## ✅ Чеклист

- [ ] Я понимаю `package.json` как манифест проекта
- [ ] Я умею читать и запускать scripts осмысленно
- [ ] Я различаю `dependencies`, `devDependencies` и `peerDependencies`
- [ ] Я понимаю ценность lockfile discipline
- [ ] Я не считаю scripts случайным набором команд

---

## Следующий шаг развития

- Конкретная реализация: [npm / pnpm / yarn: `package.json`, scripts и lockfile](../package-manager/7.1-package-json-scripts-и-lockfile.md)

---

## 📚 Полезные материалы

- [npm: package.json](https://docs.npmjs.com/cli/configuring-npm/package-json)
- [pnpm: package.json](https://pnpm.io/package_json)
- [Yarn Manifest](https://yarnpkg.com/configuration/manifest)


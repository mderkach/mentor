# 3.4 Массивы и преобразование данных

- Домен: **JavaScript**
- Атомарных навыков: **85**
- Тип: **Hard**
- Технология: **JavaScript**
- Описание технологии: Язык программирования для логики, поведения интерфейса и работы с API браузера.

## Навыки

| # | Навык | Описание | Индикатор развития | Критерии успеха | Проверка | Инструмент развития |
|---|---|---|---|---|---|---|
| 1 | array methods: map — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: map» в JavaScript. | Не может объяснить, как работает «array methods: map», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: map» в коде без догадок. | Коротко объяснить «array methods: map» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 2 | array methods: map — практическое применение | Умеет применять «array methods: map» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: map» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: map» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: map». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 3 | array methods: map — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: map». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: map» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: map». | Найти и исправить баг, связанный с темой «array methods: map». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 4 | array methods: map — отладка и производительность | Умеет отлаживать поведение «array methods: map» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: map», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: map». | Разобрать кейс в DevTools или на ревью по теме «array methods: map». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 5 | array methods: map — проектные практики | Понимает, как «array methods: map» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: map» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: map» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: map». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 6 | array methods: filter — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: filter» в JavaScript. | Не может объяснить, как работает «array methods: filter», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: filter» в коде без догадок. | Коротко объяснить «array methods: filter» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 7 | array methods: filter — практическое применение | Умеет применять «array methods: filter» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: filter» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: filter» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: filter». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 8 | array methods: filter — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: filter». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: filter» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: filter». | Найти и исправить баг, связанный с темой «array methods: filter». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 9 | array methods: filter — отладка и производительность | Умеет отлаживать поведение «array methods: filter» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: filter», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: filter». | Разобрать кейс в DevTools или на ревью по теме «array methods: filter». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 10 | array methods: filter — проектные практики | Понимает, как «array methods: filter» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: filter» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: filter» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: filter». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 11 | array methods: reduce — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: reduce» в JavaScript. | Не может объяснить, как работает «array methods: reduce», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: reduce» в коде без догадок. | Коротко объяснить «array methods: reduce» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 12 | array methods: reduce — практическое применение | Умеет применять «array methods: reduce» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: reduce» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: reduce» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: reduce». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 13 | array methods: reduce — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: reduce». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: reduce» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: reduce». | Найти и исправить баг, связанный с темой «array methods: reduce». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 14 | array methods: reduce — отладка и производительность | Умеет отлаживать поведение «array methods: reduce» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: reduce», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: reduce». | Разобрать кейс в DevTools или на ревью по теме «array methods: reduce». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 15 | array methods: reduce — проектные практики | Понимает, как «array methods: reduce» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: reduce» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: reduce» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: reduce». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 16 | array methods: forEach — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: forEach» в JavaScript. | Не может объяснить, как работает «array methods: forEach», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: forEach» в коде без догадок. | Коротко объяснить «array methods: forEach» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 17 | array methods: forEach — практическое применение | Умеет применять «array methods: forEach» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: forEach» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: forEach» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: forEach». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 18 | array methods: forEach — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: forEach». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: forEach» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: forEach». | Найти и исправить баг, связанный с темой «array methods: forEach». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 19 | array methods: forEach — отладка и производительность | Умеет отлаживать поведение «array methods: forEach» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: forEach», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: forEach». | Разобрать кейс в DevTools или на ревью по теме «array methods: forEach». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 20 | array methods: forEach — проектные практики | Понимает, как «array methods: forEach» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: forEach» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: forEach» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: forEach». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 21 | array methods: find — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: find» в JavaScript. | Не может объяснить, как работает «array methods: find», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: find» в коде без догадок. | Коротко объяснить «array methods: find» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 22 | array methods: find — практическое применение | Умеет применять «array methods: find» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: find» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: find» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: find». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 23 | array methods: find — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: find». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: find» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: find». | Найти и исправить баг, связанный с темой «array methods: find». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 24 | array methods: find — отладка и производительность | Умеет отлаживать поведение «array methods: find» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: find», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: find». | Разобрать кейс в DevTools или на ревью по теме «array methods: find». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 25 | array methods: find — проектные практики | Понимает, как «array methods: find» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: find» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: find» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: find». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 26 | array methods: some — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: some» в JavaScript. | Не может объяснить, как работает «array methods: some», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: some» в коде без догадок. | Коротко объяснить «array methods: some» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 27 | array methods: some — практическое применение | Умеет применять «array methods: some» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: some» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: some» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: some». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 28 | array methods: some — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: some». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: some» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: some». | Найти и исправить баг, связанный с темой «array methods: some». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 29 | array methods: some — отладка и производительность | Умеет отлаживать поведение «array methods: some» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: some», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: some». | Разобрать кейс в DevTools или на ревью по теме «array methods: some». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 30 | array methods: some — проектные практики | Понимает, как «array methods: some» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: some» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: some» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: some». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 31 | array methods: every — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: every» в JavaScript. | Не может объяснить, как работает «array methods: every», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: every» в коде без догадок. | Коротко объяснить «array methods: every» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 32 | array methods: every — практическое применение | Умеет применять «array methods: every» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: every» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: every» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: every». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 33 | array methods: every — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: every». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: every» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: every». | Найти и исправить баг, связанный с темой «array methods: every». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 34 | array methods: every — отладка и производительность | Умеет отлаживать поведение «array methods: every» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: every», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: every». | Разобрать кейс в DevTools или на ревью по теме «array methods: every». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 35 | array methods: every — проектные практики | Понимает, как «array methods: every» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: every» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: every» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: every». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 36 | array methods: flat — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: flat» в JavaScript. | Не может объяснить, как работает «array methods: flat», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: flat» в коде без догадок. | Коротко объяснить «array methods: flat» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 37 | array methods: flat — практическое применение | Умеет применять «array methods: flat» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: flat» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: flat» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: flat». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 38 | array methods: flat — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: flat». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: flat» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: flat». | Найти и исправить баг, связанный с темой «array methods: flat». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 39 | array methods: flat — отладка и производительность | Умеет отлаживать поведение «array methods: flat» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: flat», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: flat». | Разобрать кейс в DevTools или на ревью по теме «array methods: flat». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 40 | array methods: flat — проектные практики | Понимает, как «array methods: flat» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: flat» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: flat» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: flat». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 41 | array methods: flatMap — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: flatMap» в JavaScript. | Не может объяснить, как работает «array methods: flatMap», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: flatMap» в коде без догадок. | Коротко объяснить «array methods: flatMap» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 42 | array methods: flatMap — практическое применение | Умеет применять «array methods: flatMap» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: flatMap» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: flatMap» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: flatMap». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 43 | array methods: flatMap — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: flatMap». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: flatMap» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: flatMap». | Найти и исправить баг, связанный с темой «array methods: flatMap». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 44 | array methods: flatMap — отладка и производительность | Умеет отлаживать поведение «array methods: flatMap» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: flatMap», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: flatMap». | Разобрать кейс в DevTools или на ревью по теме «array methods: flatMap». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 45 | array methods: flatMap — проектные практики | Понимает, как «array methods: flatMap» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: flatMap» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: flatMap» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: flatMap». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 46 | array methods: sort — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: sort» в JavaScript. | Не может объяснить, как работает «array methods: sort», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: sort» в коде без догадок. | Коротко объяснить «array methods: sort» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 47 | array methods: sort — практическое применение | Умеет применять «array methods: sort» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: sort» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: sort» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: sort». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 48 | array methods: sort — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: sort». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: sort» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: sort». | Найти и исправить баг, связанный с темой «array methods: sort». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 49 | array methods: sort — отладка и производительность | Умеет отлаживать поведение «array methods: sort» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: sort», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: sort». | Разобрать кейс в DevTools или на ревью по теме «array methods: sort». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 50 | array methods: sort — проектные практики | Понимает, как «array methods: sort» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: sort» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: sort» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: sort». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 51 | array methods: toSorted — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: toSorted» в JavaScript. | Не может объяснить, как работает «array methods: toSorted», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: toSorted» в коде без догадок. | Коротко объяснить «array methods: toSorted» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 52 | array methods: toSorted — практическое применение | Умеет применять «array methods: toSorted» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: toSorted» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: toSorted» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: toSorted». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 53 | array methods: toSorted — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: toSorted». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: toSorted» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: toSorted». | Найти и исправить баг, связанный с темой «array methods: toSorted». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 54 | array methods: toSorted — отладка и производительность | Умеет отлаживать поведение «array methods: toSorted» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: toSorted», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: toSorted». | Разобрать кейс в DevTools или на ревью по теме «array methods: toSorted». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 55 | array methods: toSorted — проектные практики | Понимает, как «array methods: toSorted» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: toSorted» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: toSorted» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: toSorted». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 56 | array methods: toSpliced — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: toSpliced» в JavaScript. | Не может объяснить, как работает «array methods: toSpliced», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: toSpliced» в коде без догадок. | Коротко объяснить «array methods: toSpliced» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 57 | array methods: toSpliced — практическое применение | Умеет применять «array methods: toSpliced» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: toSpliced» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: toSpliced» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: toSpliced». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 58 | array methods: toSpliced — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: toSpliced». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: toSpliced» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: toSpliced». | Найти и исправить баг, связанный с темой «array methods: toSpliced». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 59 | array methods: toSpliced — отладка и производительность | Умеет отлаживать поведение «array methods: toSpliced» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: toSpliced», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: toSpliced». | Разобрать кейс в DevTools или на ревью по теме «array methods: toSpliced». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 60 | array methods: toSpliced — проектные практики | Понимает, как «array methods: toSpliced» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: toSpliced» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: toSpliced» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: toSpliced». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 61 | array methods: with — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: with» в JavaScript. | Не может объяснить, как работает «array methods: with», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: with» в коде без догадок. | Коротко объяснить «array methods: with» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 62 | array methods: with — практическое применение | Умеет применять «array methods: with» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: with» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: with» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: with». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 63 | array methods: with — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: with». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: with» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: with». | Найти и исправить баг, связанный с темой «array methods: with». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 64 | array methods: with — отладка и производительность | Умеет отлаживать поведение «array methods: with» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: with», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: with». | Разобрать кейс в DevTools или на ревью по теме «array methods: with». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 65 | array methods: with — проектные практики | Понимает, как «array methods: with» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: with» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: with» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: with». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 66 | array methods: slice — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: slice» в JavaScript. | Не может объяснить, как работает «array methods: slice», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: slice» в коде без догадок. | Коротко объяснить «array methods: slice» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 67 | array methods: slice — практическое применение | Умеет применять «array methods: slice» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: slice» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: slice» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: slice». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 68 | array methods: slice — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: slice». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: slice» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: slice». | Найти и исправить баг, связанный с темой «array methods: slice». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 69 | array methods: slice — отладка и производительность | Умеет отлаживать поведение «array methods: slice» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: slice», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: slice». | Разобрать кейс в DevTools или на ревью по теме «array methods: slice». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 70 | array methods: slice — проектные практики | Понимает, как «array methods: slice» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: slice» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: slice» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: slice». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 71 | array methods: splice — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: splice» в JavaScript. | Не может объяснить, как работает «array methods: splice», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: splice» в коде без догадок. | Коротко объяснить «array methods: splice» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 72 | array methods: splice — практическое применение | Умеет применять «array methods: splice» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: splice» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: splice» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: splice». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 73 | array methods: splice — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: splice». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: splice» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: splice». | Найти и исправить баг, связанный с темой «array methods: splice». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 74 | array methods: splice — отладка и производительность | Умеет отлаживать поведение «array methods: splice» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: splice», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: splice». | Разобрать кейс в DevTools или на ревью по теме «array methods: splice». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 75 | array methods: splice — проектные практики | Понимает, как «array methods: splice» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: splice» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: splice» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: splice». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 76 | array methods: push/pop — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: push/pop» в JavaScript. | Не может объяснить, как работает «array methods: push/pop», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: push/pop» в коде без догадок. | Коротко объяснить «array methods: push/pop» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 77 | array methods: push/pop — практическое применение | Умеет применять «array methods: push/pop» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: push/pop» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: push/pop» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: push/pop». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 78 | array methods: push/pop — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: push/pop». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: push/pop» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: push/pop». | Найти и исправить баг, связанный с темой «array methods: push/pop». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 79 | array methods: push/pop — отладка и производительность | Умеет отлаживать поведение «array methods: push/pop» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: push/pop», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: push/pop». | Разобрать кейс в DevTools или на ревью по теме «array methods: push/pop». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 80 | array methods: push/pop — проектные практики | Понимает, как «array methods: push/pop» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: push/pop» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: push/pop» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: push/pop». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 81 | array methods: shift/unshift — базовое понимание | Понимает синтаксис, семантику и базовое поведение «array methods: shift/unshift» в JavaScript. | Не может объяснить, как работает «array methods: shift/unshift», или путает его с похожими механизмами. | Корректно объясняет и применяет «array methods: shift/unshift» в коде без догадок. | Коротко объяснить «array methods: shift/unshift» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 82 | array methods: shift/unshift — практическое применение | Умеет применять «array methods: shift/unshift» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «array methods: shift/unshift» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «array methods: shift/unshift» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «array methods: shift/unshift». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 83 | array methods: shift/unshift — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «array methods: shift/unshift». | Не учитывает крайние случаи и типичные ошибки поведения «array methods: shift/unshift» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «array methods: shift/unshift». | Найти и исправить баг, связанный с темой «array methods: shift/unshift». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 84 | array methods: shift/unshift — отладка и производительность | Умеет отлаживать поведение «array methods: shift/unshift» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «array methods: shift/unshift», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «array methods: shift/unshift». | Разобрать кейс в DevTools или на ревью по теме «array methods: shift/unshift». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 85 | array methods: shift/unshift — проектные практики | Понимает, как «array methods: shift/unshift» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «array methods: shift/unshift» вразнобой и создаёт неоднородный стиль в проекте. | Использует «array methods: shift/unshift» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «array methods: shift/unshift». | Дока / практика / код-ревью / мини-проект / документация инструмента. |

---

[← К разделу JavaScript](./README.md)

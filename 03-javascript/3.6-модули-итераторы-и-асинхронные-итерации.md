# 3.6 Модули, итераторы и асинхронные итерации

- Домен: **JavaScript**
- Атомарных навыков: **45**
- Тип: **Hard**
- Технология: **JavaScript**
- Описание технологии: Язык программирования для логики, поведения интерфейса и работы с API браузера.

## Навыки

| # | Навык | Описание | Индикатор развития | Критерии успеха | Проверка | Инструмент развития |
|---|---|---|---|---|---|---|
| 1 | modules ESM — базовое понимание | Понимает синтаксис, семантику и базовое поведение «modules ESM» в JavaScript. | Не может объяснить, как работает «modules ESM», или путает его с похожими механизмами. | Корректно объясняет и применяет «modules ESM» в коде без догадок. | Коротко объяснить «modules ESM» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 2 | modules ESM — практическое применение | Умеет применять «modules ESM» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «modules ESM» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «modules ESM» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «modules ESM». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 3 | modules ESM — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «modules ESM». | Не учитывает крайние случаи и типичные ошибки поведения «modules ESM» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «modules ESM». | Найти и исправить баг, связанный с темой «modules ESM». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 4 | modules ESM — отладка и производительность | Умеет отлаживать поведение «modules ESM» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «modules ESM», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «modules ESM». | Разобрать кейс в DevTools или на ревью по теме «modules ESM». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 5 | modules ESM — проектные практики | Понимает, как «modules ESM» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «modules ESM» вразнобой и создаёт неоднородный стиль в проекте. | Использует «modules ESM» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «modules ESM». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 6 | import/export — базовое понимание | Понимает синтаксис, семантику и базовое поведение «import/export» в JavaScript. | Не может объяснить, как работает «import/export», или путает его с похожими механизмами. | Корректно объясняет и применяет «import/export» в коде без догадок. | Коротко объяснить «import/export» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 7 | import/export — практическое применение | Умеет применять «import/export» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «import/export» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «import/export» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «import/export». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 8 | import/export — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «import/export». | Не учитывает крайние случаи и типичные ошибки поведения «import/export» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «import/export». | Найти и исправить баг, связанный с темой «import/export». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 9 | import/export — отладка и производительность | Умеет отлаживать поведение «import/export» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «import/export», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «import/export». | Разобрать кейс в DevTools или на ревью по теме «import/export». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 10 | import/export — проектные практики | Понимает, как «import/export» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «import/export» вразнобой и создаёт неоднородный стиль в проекте. | Использует «import/export» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «import/export». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 11 | dynamic import() — базовое понимание | Понимает синтаксис, семантику и базовое поведение «dynamic import()» в JavaScript. | Не может объяснить, как работает «dynamic import()», или путает его с похожими механизмами. | Корректно объясняет и применяет «dynamic import()» в коде без догадок. | Коротко объяснить «dynamic import()» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 12 | dynamic import() — практическое применение | Умеет применять «dynamic import()» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «dynamic import()» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «dynamic import()» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «dynamic import()». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 13 | dynamic import() — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «dynamic import()». | Не учитывает крайние случаи и типичные ошибки поведения «dynamic import()» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «dynamic import()». | Найти и исправить баг, связанный с темой «dynamic import()». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 14 | dynamic import() — отладка и производительность | Умеет отлаживать поведение «dynamic import()» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «dynamic import()», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «dynamic import()». | Разобрать кейс в DevTools или на ревью по теме «dynamic import()». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 15 | dynamic import() — проектные практики | Понимает, как «dynamic import()» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «dynamic import()» вразнобой и создаёт неоднородный стиль в проекте. | Использует «dynamic import()» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «dynamic import()». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 16 | top-level await — базовое понимание | Понимает синтаксис, семантику и базовое поведение «top-level await» в JavaScript. | Не может объяснить, как работает «top-level await», или путает его с похожими механизмами. | Корректно объясняет и применяет «top-level await» в коде без догадок. | Коротко объяснить «top-level await» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 17 | top-level await — практическое применение | Умеет применять «top-level await» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «top-level await» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «top-level await» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «top-level await». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 18 | top-level await — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «top-level await». | Не учитывает крайние случаи и типичные ошибки поведения «top-level await» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «top-level await». | Найти и исправить баг, связанный с темой «top-level await». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 19 | top-level await — отладка и производительность | Умеет отлаживать поведение «top-level await» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «top-level await», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «top-level await». | Разобрать кейс в DevTools или на ревью по теме «top-level await». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 20 | top-level await — проектные практики | Понимает, как «top-level await» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «top-level await» вразнобой и создаёт неоднородный стиль в проекте. | Использует «top-level await» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «top-level await». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 21 | import.meta — базовое понимание | Понимает синтаксис, семантику и базовое поведение «import.meta» в JavaScript. | Не может объяснить, как работает «import.meta», или путает его с похожими механизмами. | Корректно объясняет и применяет «import.meta» в коде без догадок. | Коротко объяснить «import.meta» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 22 | import.meta — практическое применение | Умеет применять «import.meta» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «import.meta» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «import.meta» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «import.meta». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 23 | import.meta — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «import.meta». | Не учитывает крайние случаи и типичные ошибки поведения «import.meta» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «import.meta». | Найти и исправить баг, связанный с темой «import.meta». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 24 | import.meta — отладка и производительность | Умеет отлаживать поведение «import.meta» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «import.meta», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «import.meta». | Разобрать кейс в DevTools или на ревью по теме «import.meta». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 25 | import.meta — проектные практики | Понимает, как «import.meta» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «import.meta» вразнобой и создаёт неоднородный стиль в проекте. | Использует «import.meta» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «import.meta». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 26 | iterators — базовое понимание | Понимает синтаксис, семантику и базовое поведение «iterators» в JavaScript. | Не может объяснить, как работает «iterators», или путает его с похожими механизмами. | Корректно объясняет и применяет «iterators» в коде без догадок. | Коротко объяснить «iterators» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 27 | iterators — практическое применение | Умеет применять «iterators» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «iterators» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «iterators» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «iterators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 28 | iterators — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «iterators». | Не учитывает крайние случаи и типичные ошибки поведения «iterators» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «iterators». | Найти и исправить баг, связанный с темой «iterators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 29 | iterators — отладка и производительность | Умеет отлаживать поведение «iterators» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «iterators», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «iterators». | Разобрать кейс в DevTools или на ревью по теме «iterators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 30 | iterators — проектные практики | Понимает, как «iterators» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «iterators» вразнобой и создаёт неоднородный стиль в проекте. | Использует «iterators» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «iterators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 31 | generators — базовое понимание | Понимает синтаксис, семантику и базовое поведение «generators» в JavaScript. | Не может объяснить, как работает «generators», или путает его с похожими механизмами. | Корректно объясняет и применяет «generators» в коде без догадок. | Коротко объяснить «generators» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 32 | generators — практическое применение | Умеет применять «generators» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «generators» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «generators» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «generators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 33 | generators — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «generators». | Не учитывает крайние случаи и типичные ошибки поведения «generators» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «generators». | Найти и исправить баг, связанный с темой «generators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 34 | generators — отладка и производительность | Умеет отлаживать поведение «generators» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «generators», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «generators». | Разобрать кейс в DevTools или на ревью по теме «generators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 35 | generators — проектные практики | Понимает, как «generators» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «generators» вразнобой и создаёт неоднородный стиль в проекте. | Использует «generators» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «generators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 36 | async iterators — базовое понимание | Понимает синтаксис, семантику и базовое поведение «async iterators» в JavaScript. | Не может объяснить, как работает «async iterators», или путает его с похожими механизмами. | Корректно объясняет и применяет «async iterators» в коде без догадок. | Коротко объяснить «async iterators» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 37 | async iterators — практическое применение | Умеет применять «async iterators» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «async iterators» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «async iterators» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «async iterators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 38 | async iterators — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «async iterators». | Не учитывает крайние случаи и типичные ошибки поведения «async iterators» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «async iterators». | Найти и исправить баг, связанный с темой «async iterators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 39 | async iterators — отладка и производительность | Умеет отлаживать поведение «async iterators» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «async iterators», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «async iterators». | Разобрать кейс в DevTools или на ревью по теме «async iterators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 40 | async iterators — проектные практики | Понимает, как «async iterators» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «async iterators» вразнобой и создаёт неоднородный стиль в проекте. | Использует «async iterators» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «async iterators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 41 | for await...of — базовое понимание | Понимает синтаксис, семантику и базовое поведение «for await...of» в JavaScript. | Не может объяснить, как работает «for await...of», или путает его с похожими механизмами. | Корректно объясняет и применяет «for await...of» в коде без догадок. | Коротко объяснить «for await...of» и показать минимальный пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 42 | for await...of — практическое применение | Умеет применять «for await...of» в прикладном коде, не ухудшая читаемость и надёжность решения. | Использует «for await...of» случайно, без понимания последствий для читаемости, поддержки или производительности. | Использует «for await...of» там, где он действительно упрощает решение и делает код надёжнее. | Решить задачу на кодирование, где ключевой инструмент — «for await...of». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 43 | for await...of — ошибки и edge cases | Знает ловушки, скрытые ограничения и типовые ошибки, связанные с «for await...of». | Не учитывает крайние случаи и типичные ошибки поведения «for await...of» в рантайме. | Предугадывает крайние случаи и пишет код, устойчивый к ошибкам вокруг «for await...of». | Найти и исправить баг, связанный с темой «for await...of». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 44 | for await...of — отладка и производительность | Умеет отлаживать поведение «for await...of» и понимает его влияние на стек вызовов, память или производительность. | Не может локализовать проблему, связанную с «for await...of», или не понимает её влияние на runtime. | Локализует проблему, измеряет влияние и выбирает безопасный вариант использования «for await...of». | Разобрать кейс в DevTools или на ревью по теме «for await...of». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 45 | for await...of — проектные практики | Понимает, как «for await...of» использовать в кодовой базе команды: с тестами, соглашениями и единым стилем. | Применяет «for await...of» вразнобой и создаёт неоднородный стиль в проекте. | Использует «for await...of» последовательно и в рамках agreed conventions команды. | Предложить проектное правило или guideline по использованию «for await...of». | Дока / практика / код-ревью / мини-проект / документация инструмента. |

---

[← К разделу JavaScript](./README.md)

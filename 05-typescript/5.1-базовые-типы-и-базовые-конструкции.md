# 5.1 Базовые типы и базовые конструкции

- Домен: **TypeScript**
- Атомарных навыков: **50**
- Тип: **Hard**
- Технология: **TypeScript**
- Описание технологии: Надмножество JavaScript со статической типизацией и типовыми контрактами.

## Навыки

| # | Навык | Описание | Индикатор развития | Критерии успеха | Проверка | Инструмент развития |
|---|---|---|---|---|---|---|
| 1 | unknown — базовое понимание | Понимает смысл типовой конструкции «unknown» и то, какую задачу она решает в TypeScript. | Использует «unknown» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «unknown» и объясняет его ценность для устойчивости кода. | Пояснить «unknown» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 2 | unknown — применение в модели данных | Умеет применять «unknown» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «unknown» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «unknown» для более точной и масштабируемой модели данных. | Описать модель данных, где без «unknown» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 3 | unknown — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «unknown». | Переоценивает возможности «unknown» и не замечает дыр в типовой модели. | Учитывает ограничения «unknown» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «unknown». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 4 | unknown — проектная интеграция | Понимает, как «unknown» использовать в команде: в shared types, public API и библиотечном коде. | Использует «unknown» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «unknown» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «unknown» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 5 | unknown — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «unknown» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «unknown». | Разобрать ошибку компилятора или линтера по теме «unknown». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 6 | any — базовое понимание | Понимает смысл типовой конструкции «any» и то, какую задачу она решает в TypeScript. | Использует «any» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «any» и объясняет его ценность для устойчивости кода. | Пояснить «any» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 7 | any — применение в модели данных | Умеет применять «any» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «any» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «any» для более точной и масштабируемой модели данных. | Описать модель данных, где без «any» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 8 | any — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «any». | Переоценивает возможности «any» и не замечает дыр в типовой модели. | Учитывает ограничения «any» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «any». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 9 | any — проектная интеграция | Понимает, как «any» использовать в команде: в shared types, public API и библиотечном коде. | Использует «any» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «any» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «any» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 10 | any — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «any» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «any». | Разобрать ошибку компилятора или линтера по теме «any». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 11 | never — базовое понимание | Понимает смысл типовой конструкции «never» и то, какую задачу она решает в TypeScript. | Использует «never» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «never» и объясняет его ценность для устойчивости кода. | Пояснить «never» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 12 | never — применение в модели данных | Умеет применять «never» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «never» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «never» для более точной и масштабируемой модели данных. | Описать модель данных, где без «never» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 13 | never — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «never». | Переоценивает возможности «never» и не замечает дыр в типовой модели. | Учитывает ограничения «never» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «never». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 14 | never — проектная интеграция | Понимает, как «never» использовать в команде: в shared types, public API и библиотечном коде. | Использует «never» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «never» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «never» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 15 | never — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «never» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «never». | Разобрать ошибку компилятора или линтера по теме «never». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 16 | void — базовое понимание | Понимает смысл типовой конструкции «void» и то, какую задачу она решает в TypeScript. | Использует «void» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «void» и объясняет его ценность для устойчивости кода. | Пояснить «void» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 17 | void — применение в модели данных | Умеет применять «void» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «void» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «void» для более точной и масштабируемой модели данных. | Описать модель данных, где без «void» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 18 | void — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «void». | Переоценивает возможности «void» и не замечает дыр в типовой модели. | Учитывает ограничения «void» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «void». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 19 | void — проектная интеграция | Понимает, как «void» использовать в команде: в shared types, public API и библиотечном коде. | Использует «void» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «void» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «void» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 20 | void — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «void» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «void». | Разобрать ошибку компилятора или линтера по теме «void». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 21 | literal types — базовое понимание | Понимает смысл типовой конструкции «literal types» и то, какую задачу она решает в TypeScript. | Использует «literal types» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «literal types» и объясняет его ценность для устойчивости кода. | Пояснить «literal types» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 22 | literal types — применение в модели данных | Умеет применять «literal types» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «literal types» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «literal types» для более точной и масштабируемой модели данных. | Описать модель данных, где без «literal types» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 23 | literal types — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «literal types». | Переоценивает возможности «literal types» и не замечает дыр в типовой модели. | Учитывает ограничения «literal types» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «literal types». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 24 | literal types — проектная интеграция | Понимает, как «literal types» использовать в команде: в shared types, public API и библиотечном коде. | Использует «literal types» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «literal types» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «literal types» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 25 | literal types — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «literal types» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «literal types». | Разобрать ошибку компилятора или линтера по теме «literal types». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 26 | union types — базовое понимание | Понимает смысл типовой конструкции «union types» и то, какую задачу она решает в TypeScript. | Использует «union types» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «union types» и объясняет его ценность для устойчивости кода. | Пояснить «union types» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 27 | union types — применение в модели данных | Умеет применять «union types» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «union types» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «union types» для более точной и масштабируемой модели данных. | Описать модель данных, где без «union types» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 28 | union types — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «union types». | Переоценивает возможности «union types» и не замечает дыр в типовой модели. | Учитывает ограничения «union types» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «union types». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 29 | union types — проектная интеграция | Понимает, как «union types» использовать в команде: в shared types, public API и библиотечном коде. | Использует «union types» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «union types» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «union types» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 30 | union types — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «union types» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «union types». | Разобрать ошибку компилятора или линтера по теме «union types». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 31 | intersection types — базовое понимание | Понимает смысл типовой конструкции «intersection types» и то, какую задачу она решает в TypeScript. | Использует «intersection types» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «intersection types» и объясняет его ценность для устойчивости кода. | Пояснить «intersection types» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 32 | intersection types — применение в модели данных | Умеет применять «intersection types» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «intersection types» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «intersection types» для более точной и масштабируемой модели данных. | Описать модель данных, где без «intersection types» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 33 | intersection types — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «intersection types». | Переоценивает возможности «intersection types» и не замечает дыр в типовой модели. | Учитывает ограничения «intersection types» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «intersection types». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 34 | intersection types — проектная интеграция | Понимает, как «intersection types» использовать в команде: в shared types, public API и библиотечном коде. | Использует «intersection types» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «intersection types» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «intersection types» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 35 | intersection types — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «intersection types» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «intersection types». | Разобрать ошибку компилятора или линтера по теме «intersection types». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 36 | tuple types — базовое понимание | Понимает смысл типовой конструкции «tuple types» и то, какую задачу она решает в TypeScript. | Использует «tuple types» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «tuple types» и объясняет его ценность для устойчивости кода. | Пояснить «tuple types» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 37 | tuple types — применение в модели данных | Умеет применять «tuple types» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «tuple types» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «tuple types» для более точной и масштабируемой модели данных. | Описать модель данных, где без «tuple types» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 38 | tuple types — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «tuple types». | Переоценивает возможности «tuple types» и не замечает дыр в типовой модели. | Учитывает ограничения «tuple types» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «tuple types». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 39 | tuple types — проектная интеграция | Понимает, как «tuple types» использовать в команде: в shared types, public API и библиотечном коде. | Использует «tuple types» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «tuple types» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «tuple types» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 40 | tuple types — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «tuple types» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «tuple types». | Разобрать ошибку компилятора или линтера по теме «tuple types». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 41 | readonly arrays — базовое понимание | Понимает смысл типовой конструкции «readonly arrays» и то, какую задачу она решает в TypeScript. | Использует «readonly arrays» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «readonly arrays» и объясняет его ценность для устойчивости кода. | Пояснить «readonly arrays» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 42 | readonly arrays — применение в модели данных | Умеет применять «readonly arrays» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «readonly arrays» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «readonly arrays» для более точной и масштабируемой модели данных. | Описать модель данных, где без «readonly arrays» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 43 | readonly arrays — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «readonly arrays». | Переоценивает возможности «readonly arrays» и не замечает дыр в типовой модели. | Учитывает ограничения «readonly arrays» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «readonly arrays». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 44 | readonly arrays — проектная интеграция | Понимает, как «readonly arrays» использовать в команде: в shared types, public API и библиотечном коде. | Использует «readonly arrays» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «readonly arrays» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «readonly arrays» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 45 | readonly arrays — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «readonly arrays» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «readonly arrays». | Разобрать ошибку компилятора или линтера по теме «readonly arrays». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 46 | readonly tuples — базовое понимание | Понимает смысл типовой конструкции «readonly tuples» и то, какую задачу она решает в TypeScript. | Использует «readonly tuples» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «readonly tuples» и объясняет его ценность для устойчивости кода. | Пояснить «readonly tuples» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 47 | readonly tuples — применение в модели данных | Умеет применять «readonly tuples» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «readonly tuples» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «readonly tuples» для более точной и масштабируемой модели данных. | Описать модель данных, где без «readonly tuples» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 48 | readonly tuples — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «readonly tuples». | Переоценивает возможности «readonly tuples» и не замечает дыр в типовой модели. | Учитывает ограничения «readonly tuples» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «readonly tuples». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 49 | readonly tuples — проектная интеграция | Понимает, как «readonly tuples» использовать в команде: в shared types, public API и библиотечном коде. | Использует «readonly tuples» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «readonly tuples» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «readonly tuples» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 50 | readonly tuples — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «readonly tuples» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «readonly tuples». | Разобрать ошибку компилятора или линтера по теме «readonly tuples». | Дока / практика / код-ревью / мини-проект / документация инструмента. |

---

[← К разделу TypeScript](./README.md)

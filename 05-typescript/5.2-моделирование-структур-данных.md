# 5.2 Моделирование структур данных

- Домен: **TypeScript**
- Атомарных навыков: **45**
- Тип: **Hard**
- Технология: **TypeScript**
- Описание технологии: Надмножество JavaScript со статической типизацией и типовыми контрактами.

## Навыки

| # | Навык | Описание | Индикатор развития | Критерии успеха | Проверка | Инструмент развития |
|---|---|---|---|---|---|---|
| 1 | type alias — базовое понимание | Понимает смысл типовой конструкции «type alias» и то, какую задачу она решает в TypeScript. | Использует «type alias» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «type alias» и объясняет его ценность для устойчивости кода. | Пояснить «type alias» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 2 | type alias — применение в модели данных | Умеет применять «type alias» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «type alias» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «type alias» для более точной и масштабируемой модели данных. | Описать модель данных, где без «type alias» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 3 | type alias — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «type alias». | Переоценивает возможности «type alias» и не замечает дыр в типовой модели. | Учитывает ограничения «type alias» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «type alias». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 4 | type alias — проектная интеграция | Понимает, как «type alias» использовать в команде: в shared types, public API и библиотечном коде. | Использует «type alias» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «type alias» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «type alias» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 5 | type alias — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «type alias» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «type alias». | Разобрать ошибку компилятора или линтера по теме «type alias». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 6 | interface — базовое понимание | Понимает смысл типовой конструкции «interface» и то, какую задачу она решает в TypeScript. | Использует «interface» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «interface» и объясняет его ценность для устойчивости кода. | Пояснить «interface» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 7 | interface — применение в модели данных | Умеет применять «interface» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «interface» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «interface» для более точной и масштабируемой модели данных. | Описать модель данных, где без «interface» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 8 | interface — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «interface». | Переоценивает возможности «interface» и не замечает дыр в типовой модели. | Учитывает ограничения «interface» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «interface». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 9 | interface — проектная интеграция | Понимает, как «interface» использовать в команде: в shared types, public API и библиотечном коде. | Использует «interface» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «interface» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «interface» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 10 | interface — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «interface» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «interface». | Разобрать ошибку компилятора или линтера по теме «interface». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 11 | interface extension — базовое понимание | Понимает смысл типовой конструкции «interface extension» и то, какую задачу она решает в TypeScript. | Использует «interface extension» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «interface extension» и объясняет его ценность для устойчивости кода. | Пояснить «interface extension» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 12 | interface extension — применение в модели данных | Умеет применять «interface extension» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «interface extension» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «interface extension» для более точной и масштабируемой модели данных. | Описать модель данных, где без «interface extension» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 13 | interface extension — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «interface extension». | Переоценивает возможности «interface extension» и не замечает дыр в типовой модели. | Учитывает ограничения «interface extension» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «interface extension». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 14 | interface extension — проектная интеграция | Понимает, как «interface extension» использовать в команде: в shared types, public API и библиотечном коде. | Использует «interface extension» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «interface extension» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «interface extension» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 15 | interface extension — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «interface extension» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «interface extension». | Разобрать ошибку компилятора или линтера по теме «interface extension». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 16 | declaration merging — базовое понимание | Понимает смысл типовой конструкции «declaration merging» и то, какую задачу она решает в TypeScript. | Использует «declaration merging» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «declaration merging» и объясняет его ценность для устойчивости кода. | Пояснить «declaration merging» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 17 | declaration merging — применение в модели данных | Умеет применять «declaration merging» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «declaration merging» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «declaration merging» для более точной и масштабируемой модели данных. | Описать модель данных, где без «declaration merging» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 18 | declaration merging — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «declaration merging». | Переоценивает возможности «declaration merging» и не замечает дыр в типовой модели. | Учитывает ограничения «declaration merging» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «declaration merging». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 19 | declaration merging — проектная интеграция | Понимает, как «declaration merging» использовать в команде: в shared types, public API и библиотечном коде. | Использует «declaration merging» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «declaration merging» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «declaration merging» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 20 | declaration merging — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «declaration merging» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «declaration merging». | Разобрать ошибку компилятора или линтера по теме «declaration merging». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 21 | optional properties — базовое понимание | Понимает смысл типовой конструкции «optional properties» и то, какую задачу она решает в TypeScript. | Использует «optional properties» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «optional properties» и объясняет его ценность для устойчивости кода. | Пояснить «optional properties» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 22 | optional properties — применение в модели данных | Умеет применять «optional properties» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «optional properties» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «optional properties» для более точной и масштабируемой модели данных. | Описать модель данных, где без «optional properties» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 23 | optional properties — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «optional properties». | Переоценивает возможности «optional properties» и не замечает дыр в типовой модели. | Учитывает ограничения «optional properties» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «optional properties». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 24 | optional properties — проектная интеграция | Понимает, как «optional properties» использовать в команде: в shared types, public API и библиотечном коде. | Использует «optional properties» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «optional properties» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «optional properties» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 25 | optional properties — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «optional properties» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «optional properties». | Разобрать ошибку компилятора или линтера по теме «optional properties». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 26 | index signatures — базовое понимание | Понимает смысл типовой конструкции «index signatures» и то, какую задачу она решает в TypeScript. | Использует «index signatures» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «index signatures» и объясняет его ценность для устойчивости кода. | Пояснить «index signatures» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 27 | index signatures — применение в модели данных | Умеет применять «index signatures» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «index signatures» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «index signatures» для более точной и масштабируемой модели данных. | Описать модель данных, где без «index signatures» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 28 | index signatures — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «index signatures». | Переоценивает возможности «index signatures» и не замечает дыр в типовой модели. | Учитывает ограничения «index signatures» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «index signatures». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 29 | index signatures — проектная интеграция | Понимает, как «index signatures» использовать в команде: в shared types, public API и библиотечном коде. | Использует «index signatures» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «index signatures» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «index signatures» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 30 | index signatures — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «index signatures» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «index signatures». | Разобрать ошибку компилятора или линтера по теме «index signatures». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 31 | keyof — базовое понимание | Понимает смысл типовой конструкции «keyof» и то, какую задачу она решает в TypeScript. | Использует «keyof» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «keyof» и объясняет его ценность для устойчивости кода. | Пояснить «keyof» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 32 | keyof — применение в модели данных | Умеет применять «keyof» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «keyof» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «keyof» для более точной и масштабируемой модели данных. | Описать модель данных, где без «keyof» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 33 | keyof — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «keyof». | Переоценивает возможности «keyof» и не замечает дыр в типовой модели. | Учитывает ограничения «keyof» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «keyof». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 34 | keyof — проектная интеграция | Понимает, как «keyof» использовать в команде: в shared types, public API и библиотечном коде. | Использует «keyof» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «keyof» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «keyof» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 35 | keyof — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «keyof» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «keyof». | Разобрать ошибку компилятора или линтера по теме «keyof». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 36 | typeof in types — базовое понимание | Понимает смысл типовой конструкции «typeof in types» и то, какую задачу она решает в TypeScript. | Использует «typeof in types» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «typeof in types» и объясняет его ценность для устойчивости кода. | Пояснить «typeof in types» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 37 | typeof in types — применение в модели данных | Умеет применять «typeof in types» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «typeof in types» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «typeof in types» для более точной и масштабируемой модели данных. | Описать модель данных, где без «typeof in types» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 38 | typeof in types — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «typeof in types». | Переоценивает возможности «typeof in types» и не замечает дыр в типовой модели. | Учитывает ограничения «typeof in types» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «typeof in types». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 39 | typeof in types — проектная интеграция | Понимает, как «typeof in types» использовать в команде: в shared types, public API и библиотечном коде. | Использует «typeof in types» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «typeof in types» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «typeof in types» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 40 | typeof in types — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «typeof in types» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «typeof in types». | Разобрать ошибку компилятора или линтера по теме «typeof in types». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 41 | indexed access types — базовое понимание | Понимает смысл типовой конструкции «indexed access types» и то, какую задачу она решает в TypeScript. | Использует «indexed access types» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «indexed access types» и объясняет его ценность для устойчивости кода. | Пояснить «indexed access types» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 42 | indexed access types — применение в модели данных | Умеет применять «indexed access types» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «indexed access types» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «indexed access types» для более точной и масштабируемой модели данных. | Описать модель данных, где без «indexed access types» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 43 | indexed access types — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «indexed access types». | Переоценивает возможности «indexed access types» и не замечает дыр в типовой модели. | Учитывает ограничения «indexed access types» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «indexed access types». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 44 | indexed access types — проектная интеграция | Понимает, как «indexed access types» использовать в команде: в shared types, public API и библиотечном коде. | Использует «indexed access types» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «indexed access types» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «indexed access types» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 45 | indexed access types — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «indexed access types» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «indexed access types». | Разобрать ошибку компилятора или линтера по теме «indexed access types». | Дока / практика / код-ревью / мини-проект / документация инструмента. |

---

[← К разделу TypeScript](./README.md)

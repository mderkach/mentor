# 5.7 Компилятор, tsconfig и сборка

- Домен: **TypeScript**
- Атомарных навыков: **80**
- Тип: **Hard**
- Технология: **TypeScript**
- Описание технологии: Надмножество JavaScript со статической типизацией и типовыми контрактами.

## Навыки

| # | Навык | Описание | Индикатор развития | Критерии успеха | Проверка | Инструмент развития |
|---|---|---|---|---|---|---|
| 1 | tsconfig: strict — базовое понимание | Понимает смысл типовой конструкции «tsconfig: strict» и то, какую задачу она решает в TypeScript. | Использует «tsconfig: strict» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «tsconfig: strict» и объясняет его ценность для устойчивости кода. | Пояснить «tsconfig: strict» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 2 | tsconfig: strict — применение в модели данных | Умеет применять «tsconfig: strict» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «tsconfig: strict» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «tsconfig: strict» для более точной и масштабируемой модели данных. | Описать модель данных, где без «tsconfig: strict» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 3 | tsconfig: strict — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «tsconfig: strict». | Переоценивает возможности «tsconfig: strict» и не замечает дыр в типовой модели. | Учитывает ограничения «tsconfig: strict» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «tsconfig: strict». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 4 | tsconfig: strict — проектная интеграция | Понимает, как «tsconfig: strict» использовать в команде: в shared types, public API и библиотечном коде. | Использует «tsconfig: strict» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «tsconfig: strict» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «tsconfig: strict» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 5 | tsconfig: strict — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «tsconfig: strict» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «tsconfig: strict». | Разобрать ошибку компилятора или линтера по теме «tsconfig: strict». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 6 | tsconfig: noImplicitAny — базовое понимание | Понимает смысл типовой конструкции «tsconfig: noImplicitAny» и то, какую задачу она решает в TypeScript. | Использует «tsconfig: noImplicitAny» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «tsconfig: noImplicitAny» и объясняет его ценность для устойчивости кода. | Пояснить «tsconfig: noImplicitAny» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 7 | tsconfig: noImplicitAny — применение в модели данных | Умеет применять «tsconfig: noImplicitAny» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «tsconfig: noImplicitAny» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «tsconfig: noImplicitAny» для более точной и масштабируемой модели данных. | Описать модель данных, где без «tsconfig: noImplicitAny» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 8 | tsconfig: noImplicitAny — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «tsconfig: noImplicitAny». | Переоценивает возможности «tsconfig: noImplicitAny» и не замечает дыр в типовой модели. | Учитывает ограничения «tsconfig: noImplicitAny» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «tsconfig: noImplicitAny». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 9 | tsconfig: noImplicitAny — проектная интеграция | Понимает, как «tsconfig: noImplicitAny» использовать в команде: в shared types, public API и библиотечном коде. | Использует «tsconfig: noImplicitAny» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «tsconfig: noImplicitAny» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «tsconfig: noImplicitAny» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 10 | tsconfig: noImplicitAny — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «tsconfig: noImplicitAny» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «tsconfig: noImplicitAny». | Разобрать ошибку компилятора или линтера по теме «tsconfig: noImplicitAny». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 11 | tsconfig: strictNullChecks — базовое понимание | Понимает смысл типовой конструкции «tsconfig: strictNullChecks» и то, какую задачу она решает в TypeScript. | Использует «tsconfig: strictNullChecks» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «tsconfig: strictNullChecks» и объясняет его ценность для устойчивости кода. | Пояснить «tsconfig: strictNullChecks» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 12 | tsconfig: strictNullChecks — применение в модели данных | Умеет применять «tsconfig: strictNullChecks» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «tsconfig: strictNullChecks» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «tsconfig: strictNullChecks» для более точной и масштабируемой модели данных. | Описать модель данных, где без «tsconfig: strictNullChecks» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 13 | tsconfig: strictNullChecks — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «tsconfig: strictNullChecks». | Переоценивает возможности «tsconfig: strictNullChecks» и не замечает дыр в типовой модели. | Учитывает ограничения «tsconfig: strictNullChecks» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «tsconfig: strictNullChecks». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 14 | tsconfig: strictNullChecks — проектная интеграция | Понимает, как «tsconfig: strictNullChecks» использовать в команде: в shared types, public API и библиотечном коде. | Использует «tsconfig: strictNullChecks» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «tsconfig: strictNullChecks» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «tsconfig: strictNullChecks» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 15 | tsconfig: strictNullChecks — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «tsconfig: strictNullChecks» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «tsconfig: strictNullChecks». | Разобрать ошибку компилятора или линтера по теме «tsconfig: strictNullChecks». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 16 | tsconfig: paths — базовое понимание | Понимает смысл типовой конструкции «tsconfig: paths» и то, какую задачу она решает в TypeScript. | Использует «tsconfig: paths» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «tsconfig: paths» и объясняет его ценность для устойчивости кода. | Пояснить «tsconfig: paths» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 17 | tsconfig: paths — применение в модели данных | Умеет применять «tsconfig: paths» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «tsconfig: paths» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «tsconfig: paths» для более точной и масштабируемой модели данных. | Описать модель данных, где без «tsconfig: paths» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 18 | tsconfig: paths — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «tsconfig: paths». | Переоценивает возможности «tsconfig: paths» и не замечает дыр в типовой модели. | Учитывает ограничения «tsconfig: paths» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «tsconfig: paths». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 19 | tsconfig: paths — проектная интеграция | Понимает, как «tsconfig: paths» использовать в команде: в shared types, public API и библиотечном коде. | Использует «tsconfig: paths» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «tsconfig: paths» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «tsconfig: paths» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 20 | tsconfig: paths — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «tsconfig: paths» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «tsconfig: paths». | Разобрать ошибку компилятора или линтера по теме «tsconfig: paths». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 21 | tsconfig: baseUrl — базовое понимание | Понимает смысл типовой конструкции «tsconfig: baseUrl» и то, какую задачу она решает в TypeScript. | Использует «tsconfig: baseUrl» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «tsconfig: baseUrl» и объясняет его ценность для устойчивости кода. | Пояснить «tsconfig: baseUrl» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 22 | tsconfig: baseUrl — применение в модели данных | Умеет применять «tsconfig: baseUrl» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «tsconfig: baseUrl» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «tsconfig: baseUrl» для более точной и масштабируемой модели данных. | Описать модель данных, где без «tsconfig: baseUrl» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 23 | tsconfig: baseUrl — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «tsconfig: baseUrl». | Переоценивает возможности «tsconfig: baseUrl» и не замечает дыр в типовой модели. | Учитывает ограничения «tsconfig: baseUrl» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «tsconfig: baseUrl». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 24 | tsconfig: baseUrl — проектная интеграция | Понимает, как «tsconfig: baseUrl» использовать в команде: в shared types, public API и библиотечном коде. | Использует «tsconfig: baseUrl» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «tsconfig: baseUrl» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «tsconfig: baseUrl» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 25 | tsconfig: baseUrl — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «tsconfig: baseUrl» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «tsconfig: baseUrl». | Разобрать ошибку компилятора или линтера по теме «tsconfig: baseUrl». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 26 | tsconfig: moduleResolution — базовое понимание | Понимает смысл типовой конструкции «tsconfig: moduleResolution» и то, какую задачу она решает в TypeScript. | Использует «tsconfig: moduleResolution» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «tsconfig: moduleResolution» и объясняет его ценность для устойчивости кода. | Пояснить «tsconfig: moduleResolution» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 27 | tsconfig: moduleResolution — применение в модели данных | Умеет применять «tsconfig: moduleResolution» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «tsconfig: moduleResolution» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «tsconfig: moduleResolution» для более точной и масштабируемой модели данных. | Описать модель данных, где без «tsconfig: moduleResolution» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 28 | tsconfig: moduleResolution — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «tsconfig: moduleResolution». | Переоценивает возможности «tsconfig: moduleResolution» и не замечает дыр в типовой модели. | Учитывает ограничения «tsconfig: moduleResolution» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «tsconfig: moduleResolution». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 29 | tsconfig: moduleResolution — проектная интеграция | Понимает, как «tsconfig: moduleResolution» использовать в команде: в shared types, public API и библиотечном коде. | Использует «tsconfig: moduleResolution» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «tsconfig: moduleResolution» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «tsconfig: moduleResolution» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 30 | tsconfig: moduleResolution — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «tsconfig: moduleResolution» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «tsconfig: moduleResolution». | Разобрать ошибку компилятора или линтера по теме «tsconfig: moduleResolution». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 31 | tsconfig: composite — базовое понимание | Понимает смысл типовой конструкции «tsconfig: composite» и то, какую задачу она решает в TypeScript. | Использует «tsconfig: composite» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «tsconfig: composite» и объясняет его ценность для устойчивости кода. | Пояснить «tsconfig: composite» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 32 | tsconfig: composite — применение в модели данных | Умеет применять «tsconfig: composite» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «tsconfig: composite» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «tsconfig: composite» для более точной и масштабируемой модели данных. | Описать модель данных, где без «tsconfig: composite» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 33 | tsconfig: composite — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «tsconfig: composite». | Переоценивает возможности «tsconfig: composite» и не замечает дыр в типовой модели. | Учитывает ограничения «tsconfig: composite» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «tsconfig: composite». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 34 | tsconfig: composite — проектная интеграция | Понимает, как «tsconfig: composite» использовать в команде: в shared types, public API и библиотечном коде. | Использует «tsconfig: composite» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «tsconfig: composite» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «tsconfig: composite» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 35 | tsconfig: composite — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «tsconfig: composite» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «tsconfig: composite». | Разобрать ошибку компилятора или линтера по теме «tsconfig: composite». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 36 | tsconfig: incremental — базовое понимание | Понимает смысл типовой конструкции «tsconfig: incremental» и то, какую задачу она решает в TypeScript. | Использует «tsconfig: incremental» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «tsconfig: incremental» и объясняет его ценность для устойчивости кода. | Пояснить «tsconfig: incremental» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 37 | tsconfig: incremental — применение в модели данных | Умеет применять «tsconfig: incremental» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «tsconfig: incremental» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «tsconfig: incremental» для более точной и масштабируемой модели данных. | Описать модель данных, где без «tsconfig: incremental» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 38 | tsconfig: incremental — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «tsconfig: incremental». | Переоценивает возможности «tsconfig: incremental» и не замечает дыр в типовой модели. | Учитывает ограничения «tsconfig: incremental» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «tsconfig: incremental». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 39 | tsconfig: incremental — проектная интеграция | Понимает, как «tsconfig: incremental» использовать в команде: в shared types, public API и библиотечном коде. | Использует «tsconfig: incremental» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «tsconfig: incremental» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «tsconfig: incremental» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 40 | tsconfig: incremental — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «tsconfig: incremental» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «tsconfig: incremental». | Разобрать ошибку компилятора или линтера по теме «tsconfig: incremental». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 41 | project references — базовое понимание | Понимает смысл типовой конструкции «project references» и то, какую задачу она решает в TypeScript. | Использует «project references» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «project references» и объясняет его ценность для устойчивости кода. | Пояснить «project references» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 42 | project references — применение в модели данных | Умеет применять «project references» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «project references» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «project references» для более точной и масштабируемой модели данных. | Описать модель данных, где без «project references» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 43 | project references — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «project references». | Переоценивает возможности «project references» и не замечает дыр в типовой модели. | Учитывает ограничения «project references» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «project references». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 44 | project references — проектная интеграция | Понимает, как «project references» использовать в команде: в shared types, public API и библиотечном коде. | Использует «project references» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «project references» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «project references» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 45 | project references — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «project references» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «project references». | Разобрать ошибку компилятора или линтера по теме «project references». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 46 | declaration emit — базовое понимание | Понимает смысл типовой конструкции «declaration emit» и то, какую задачу она решает в TypeScript. | Использует «declaration emit» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «declaration emit» и объясняет его ценность для устойчивости кода. | Пояснить «declaration emit» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 47 | declaration emit — применение в модели данных | Умеет применять «declaration emit» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «declaration emit» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «declaration emit» для более точной и масштабируемой модели данных. | Описать модель данных, где без «declaration emit» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 48 | declaration emit — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «declaration emit». | Переоценивает возможности «declaration emit» и не замечает дыр в типовой модели. | Учитывает ограничения «declaration emit» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «declaration emit». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 49 | declaration emit — проектная интеграция | Понимает, как «declaration emit» использовать в команде: в shared types, public API и библиотечном коде. | Использует «declaration emit» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «declaration emit» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «declaration emit» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 50 | declaration emit — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «declaration emit» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «declaration emit». | Разобрать ошибку компилятора или линтера по теме «declaration emit». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 51 | decorators — базовое понимание | Понимает смысл типовой конструкции «decorators» и то, какую задачу она решает в TypeScript. | Использует «decorators» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «decorators» и объясняет его ценность для устойчивости кода. | Пояснить «decorators» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 52 | decorators — применение в модели данных | Умеет применять «decorators» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «decorators» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «decorators» для более точной и масштабируемой модели данных. | Описать модель данных, где без «decorators» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 53 | decorators — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «decorators». | Переоценивает возможности «decorators» и не замечает дыр в типовой модели. | Учитывает ограничения «decorators» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «decorators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 54 | decorators — проектная интеграция | Понимает, как «decorators» использовать в команде: в shared types, public API и библиотечном коде. | Использует «decorators» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «decorators» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «decorators» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 55 | decorators — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «decorators» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «decorators». | Разобрать ошибку компилятора или линтера по теме «decorators». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 56 | emit metadata basics — базовое понимание | Понимает смысл типовой конструкции «emit metadata basics» и то, какую задачу она решает в TypeScript. | Использует «emit metadata basics» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «emit metadata basics» и объясняет его ценность для устойчивости кода. | Пояснить «emit metadata basics» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 57 | emit metadata basics — применение в модели данных | Умеет применять «emit metadata basics» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «emit metadata basics» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «emit metadata basics» для более точной и масштабируемой модели данных. | Описать модель данных, где без «emit metadata basics» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 58 | emit metadata basics — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «emit metadata basics». | Переоценивает возможности «emit metadata basics» и не замечает дыр в типовой модели. | Учитывает ограничения «emit metadata basics» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «emit metadata basics». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 59 | emit metadata basics — проектная интеграция | Понимает, как «emit metadata basics» использовать в команде: в shared types, public API и библиотечном коде. | Использует «emit metadata basics» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «emit metadata basics» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «emit metadata basics» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 60 | emit metadata basics — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «emit metadata basics» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «emit metadata basics». | Разобрать ошибку компилятора или линтера по теме «emit metadata basics». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 61 | isolatedModules — базовое понимание | Понимает смысл типовой конструкции «isolatedModules» и то, какую задачу она решает в TypeScript. | Использует «isolatedModules» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «isolatedModules» и объясняет его ценность для устойчивости кода. | Пояснить «isolatedModules» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 62 | isolatedModules — применение в модели данных | Умеет применять «isolatedModules» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «isolatedModules» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «isolatedModules» для более точной и масштабируемой модели данных. | Описать модель данных, где без «isolatedModules» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 63 | isolatedModules — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «isolatedModules». | Переоценивает возможности «isolatedModules» и не замечает дыр в типовой модели. | Учитывает ограничения «isolatedModules» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «isolatedModules». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 64 | isolatedModules — проектная интеграция | Понимает, как «isolatedModules» использовать в команде: в shared types, public API и библиотечном коде. | Использует «isolatedModules» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «isolatedModules» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «isolatedModules» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 65 | isolatedModules — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «isolatedModules» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «isolatedModules». | Разобрать ошибку компилятора или линтера по теме «isolatedModules». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 66 | verbatimModuleSyntax — базовое понимание | Понимает смысл типовой конструкции «verbatimModuleSyntax» и то, какую задачу она решает в TypeScript. | Использует «verbatimModuleSyntax» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «verbatimModuleSyntax» и объясняет его ценность для устойчивости кода. | Пояснить «verbatimModuleSyntax» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 67 | verbatimModuleSyntax — применение в модели данных | Умеет применять «verbatimModuleSyntax» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «verbatimModuleSyntax» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «verbatimModuleSyntax» для более точной и масштабируемой модели данных. | Описать модель данных, где без «verbatimModuleSyntax» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 68 | verbatimModuleSyntax — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «verbatimModuleSyntax». | Переоценивает возможности «verbatimModuleSyntax» и не замечает дыр в типовой модели. | Учитывает ограничения «verbatimModuleSyntax» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «verbatimModuleSyntax». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 69 | verbatimModuleSyntax — проектная интеграция | Понимает, как «verbatimModuleSyntax» использовать в команде: в shared types, public API и библиотечном коде. | Использует «verbatimModuleSyntax» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «verbatimModuleSyntax» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «verbatimModuleSyntax» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 70 | verbatimModuleSyntax — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «verbatimModuleSyntax» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «verbatimModuleSyntax». | Разобрать ошибку компилятора или линтера по теме «verbatimModuleSyntax». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 71 | moduleDetection — базовое понимание | Понимает смысл типовой конструкции «moduleDetection» и то, какую задачу она решает в TypeScript. | Использует «moduleDetection» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «moduleDetection» и объясняет его ценность для устойчивости кода. | Пояснить «moduleDetection» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 72 | moduleDetection — применение в модели данных | Умеет применять «moduleDetection» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «moduleDetection» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «moduleDetection» для более точной и масштабируемой модели данных. | Описать модель данных, где без «moduleDetection» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 73 | moduleDetection — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «moduleDetection». | Переоценивает возможности «moduleDetection» и не замечает дыр в типовой модели. | Учитывает ограничения «moduleDetection» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «moduleDetection». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 74 | moduleDetection — проектная интеграция | Понимает, как «moduleDetection» использовать в команде: в shared types, public API и библиотечном коде. | Использует «moduleDetection» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «moduleDetection» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «moduleDetection» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 75 | moduleDetection — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «moduleDetection» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «moduleDetection». | Разобрать ошибку компилятора или линтера по теме «moduleDetection». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 76 | JS Doc type checking — базовое понимание | Понимает смысл типовой конструкции «JS Doc type checking» и то, какую задачу она решает в TypeScript. | Использует «JS Doc type checking» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «JS Doc type checking» и объясняет его ценность для устойчивости кода. | Пояснить «JS Doc type checking» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 77 | JS Doc type checking — применение в модели данных | Умеет применять «JS Doc type checking» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «JS Doc type checking» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «JS Doc type checking» для более точной и масштабируемой модели данных. | Описать модель данных, где без «JS Doc type checking» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 78 | JS Doc type checking — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «JS Doc type checking». | Переоценивает возможности «JS Doc type checking» и не замечает дыр в типовой модели. | Учитывает ограничения «JS Doc type checking» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «JS Doc type checking». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 79 | JS Doc type checking — проектная интеграция | Понимает, как «JS Doc type checking» использовать в команде: в shared types, public API и библиотечном коде. | Использует «JS Doc type checking» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «JS Doc type checking» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «JS Doc type checking» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 80 | JS Doc type checking — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «JS Doc type checking» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «JS Doc type checking». | Разобрать ошибку компилятора или линтера по теме «JS Doc type checking». | Дока / практика / код-ревью / мини-проект / документация инструмента. |

---

[← К разделу TypeScript](./README.md)

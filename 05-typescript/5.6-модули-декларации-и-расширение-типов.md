# 5.6 Модули, декларации и расширение типов

- Домен: **TypeScript**
- Атомарных навыков: **35**
- Тип: **Hard**
- Технология: **TypeScript**
- Описание технологии: Надмножество JavaScript со статической типизацией и типовыми контрактами.

## Навыки

| # | Навык | Описание | Индикатор развития | Критерии успеха | Проверка | Инструмент развития |
|---|---|---|---|---|---|---|
| 1 | enum — базовое понимание | Понимает смысл типовой конструкции «enum» и то, какую задачу она решает в TypeScript. | Использует «enum» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «enum» и объясняет его ценность для устойчивости кода. | Пояснить «enum» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 2 | enum — применение в модели данных | Умеет применять «enum» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «enum» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «enum» для более точной и масштабируемой модели данных. | Описать модель данных, где без «enum» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 3 | enum — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «enum». | Переоценивает возможности «enum» и не замечает дыр в типовой модели. | Учитывает ограничения «enum» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «enum». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 4 | enum — проектная интеграция | Понимает, как «enum» использовать в команде: в shared types, public API и библиотечном коде. | Использует «enum» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «enum» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «enum» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 5 | enum — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «enum» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «enum». | Разобрать ошибку компилятора или линтера по теме «enum». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 6 | const enum — базовое понимание | Понимает смысл типовой конструкции «const enum» и то, какую задачу она решает в TypeScript. | Использует «const enum» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «const enum» и объясняет его ценность для устойчивости кода. | Пояснить «const enum» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 7 | const enum — применение в модели данных | Умеет применять «const enum» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «const enum» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «const enum» для более точной и масштабируемой модели данных. | Описать модель данных, где без «const enum» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 8 | const enum — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «const enum». | Переоценивает возможности «const enum» и не замечает дыр в типовой модели. | Учитывает ограничения «const enum» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «const enum». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 9 | const enum — проектная интеграция | Понимает, как «const enum» использовать в команде: в shared types, public API и библиотечном коде. | Использует «const enum» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «const enum» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «const enum» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 10 | const enum — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «const enum» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «const enum». | Разобрать ошибку компилятора или линтера по теме «const enum». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 11 | namespace — базовое понимание | Понимает смысл типовой конструкции «namespace» и то, какую задачу она решает в TypeScript. | Использует «namespace» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «namespace» и объясняет его ценность для устойчивости кода. | Пояснить «namespace» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 12 | namespace — применение в модели данных | Умеет применять «namespace» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «namespace» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «namespace» для более точной и масштабируемой модели данных. | Описать модель данных, где без «namespace» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 13 | namespace — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «namespace». | Переоценивает возможности «namespace» и не замечает дыр в типовой модели. | Учитывает ограничения «namespace» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «namespace». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 14 | namespace — проектная интеграция | Понимает, как «namespace» использовать в команде: в shared types, public API и библиотечном коде. | Использует «namespace» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «namespace» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «namespace» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 15 | namespace — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «namespace» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «namespace». | Разобрать ошибку компилятора или линтера по теме «namespace». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 16 | module augmentation — базовое понимание | Понимает смысл типовой конструкции «module augmentation» и то, какую задачу она решает в TypeScript. | Использует «module augmentation» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «module augmentation» и объясняет его ценность для устойчивости кода. | Пояснить «module augmentation» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 17 | module augmentation — применение в модели данных | Умеет применять «module augmentation» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «module augmentation» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «module augmentation» для более точной и масштабируемой модели данных. | Описать модель данных, где без «module augmentation» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 18 | module augmentation — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «module augmentation». | Переоценивает возможности «module augmentation» и не замечает дыр в типовой модели. | Учитывает ограничения «module augmentation» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «module augmentation». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 19 | module augmentation — проектная интеграция | Понимает, как «module augmentation» использовать в команде: в shared types, public API и библиотечном коде. | Использует «module augmentation» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «module augmentation» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «module augmentation» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 20 | module augmentation — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «module augmentation» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «module augmentation». | Разобрать ошибку компилятора или линтера по теме «module augmentation». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 21 | ambient declarations — базовое понимание | Понимает смысл типовой конструкции «ambient declarations» и то, какую задачу она решает в TypeScript. | Использует «ambient declarations» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «ambient declarations» и объясняет его ценность для устойчивости кода. | Пояснить «ambient declarations» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 22 | ambient declarations — применение в модели данных | Умеет применять «ambient declarations» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «ambient declarations» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «ambient declarations» для более точной и масштабируемой модели данных. | Описать модель данных, где без «ambient declarations» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 23 | ambient declarations — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «ambient declarations». | Переоценивает возможности «ambient declarations» и не замечает дыр в типовой модели. | Учитывает ограничения «ambient declarations» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «ambient declarations». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 24 | ambient declarations — проектная интеграция | Понимает, как «ambient declarations» использовать в команде: в shared types, public API и библиотечном коде. | Использует «ambient declarations» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «ambient declarations» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «ambient declarations» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 25 | ambient declarations — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «ambient declarations» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «ambient declarations». | Разобрать ошибку компилятора или линтера по теме «ambient declarations». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 26 | declaration files d.ts — базовое понимание | Понимает смысл типовой конструкции «declaration files d.ts» и то, какую задачу она решает в TypeScript. | Использует «declaration files d.ts» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «declaration files d.ts» и объясняет его ценность для устойчивости кода. | Пояснить «declaration files d.ts» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 27 | declaration files d.ts — применение в модели данных | Умеет применять «declaration files d.ts» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «declaration files d.ts» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «declaration files d.ts» для более точной и масштабируемой модели данных. | Описать модель данных, где без «declaration files d.ts» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 28 | declaration files d.ts — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «declaration files d.ts». | Переоценивает возможности «declaration files d.ts» и не замечает дыр в типовой модели. | Учитывает ограничения «declaration files d.ts» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «declaration files d.ts». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 29 | declaration files d.ts — проектная интеграция | Понимает, как «declaration files d.ts» использовать в команде: в shared types, public API и библиотечном коде. | Использует «declaration files d.ts» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «declaration files d.ts» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «declaration files d.ts» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 30 | declaration files d.ts — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «declaration files d.ts» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «declaration files d.ts». | Разобрать ошибку компилятора или линтера по теме «declaration files d.ts». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 31 | triple-slash directives — базовое понимание | Понимает смысл типовой конструкции «triple-slash directives» и то, какую задачу она решает в TypeScript. | Использует «triple-slash directives» по подсказке IDE, но не понимает, зачем он нужен. | Осознанно использует «triple-slash directives» и объясняет его ценность для устойчивости кода. | Пояснить «triple-slash directives» и показать минимальный типовой пример. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 32 | triple-slash directives — применение в модели данных | Умеет применять «triple-slash directives» при моделировании данных, API-контрактов и UI-состояния. | Моделирует данные вокруг «triple-slash directives» неустойчиво: с any, дублированием типов или потерей инвариантов. | Использует «triple-slash directives» для более точной и масштабируемой модели данных. | Описать модель данных, где без «triple-slash directives» решение будет хуже. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 33 | triple-slash directives — ошибки и ограничения | Знает ограничения, источники ложной безопасности и типичные ошибки вокруг «triple-slash directives». | Переоценивает возможности «triple-slash directives» и не замечает дыр в типовой модели. | Учитывает ограничения «triple-slash directives» и не путает типовую безопасность с runtime-гарантиями. | Найти ошибку проектирования типов, связанную с «triple-slash directives». | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 34 | triple-slash directives — проектная интеграция | Понимает, как «triple-slash directives» использовать в команде: в shared types, public API и библиотечном коде. | Использует «triple-slash directives» непоследовательно и ломает читабельность или DX в кодовой базе. | Использует «triple-slash directives» последовательно и делает типовую систему проекта предсказуемой. | Предложить guideline по использованию «triple-slash directives» в кодовой базе. | Дока / практика / код-ревью / мини-проект / документация инструмента. |
| 35 | triple-slash directives — отладка и tooling | Умеет отлаживать ошибки типов, настраивать tooling и связывать «triple-slash directives» с компилятором/линтером. | Не умеет локализовать ошибку типов и не связывает её с настройками tooling. | Быстро находит источник ошибки типов и знает, какие настройки tooling влияют на «triple-slash directives». | Разобрать ошибку компилятора или линтера по теме «triple-slash directives». | Дока / практика / код-ревью / мини-проект / документация инструмента. |

---

[← К разделу TypeScript](./README.md)
